{
  "get_ai_readiness_history": {
    "name": "get_ai_readiness_history",
    "description": "Get the historical AI Readiness snapshots for a repository. Returns all snapshots ordered by date, allowing you to track improvements over time.",
    "parameters": {
      "type": "object",
      "properties": {
        "repository": {
          "type": "string",
          "description": "Repository name to get AI readiness history for (e.g., \"acme/web-app\"). Supports partial matching."
        },
        "dataSourceId": {
          "type": "string",
          "description": "Data source ID to filter by (alternative to repository name)"
        },
        "limit": {
          "type": "number",
          "description": "Maximum number of snapshots to return (default 10)"
        }
      },
      "required": []
    }
  },
  "get_ai_readiness": {
    "name": "get_ai_readiness",
    "description": "Get the latest AI Readiness snapshot for a repository. Returns the overall score, grade, dimension scores, and recommendations. Run the ai-readiness-analyzer agent first to populate this data.",
    "parameters": {
      "type": "object",
      "properties": {
        "repository": {
          "type": "string",
          "description": "Repository name to get AI readiness for (e.g., \"acme/web-app\"). Supports partial matching."
        },
        "dataSourceId": {
          "type": "string",
          "description": "Data source ID to filter by (alternative to repository name)"
        }
      },
      "required": []
    }
  },
  "get_coding_guidelines": {
    "name": "get_coding_guidelines",
    "description": "Retrieve coding guidelines and best practices from the knowledge base. Use this tool to get team-specific guidelines for code review and development standards.",
    "parameters": {
      "type": "object",
      "properties": {
        "category": {
          "type": "string",
          "description": "Filter guidelines by category (e.g., \"security\", \"error-handling\", \"code-quality\")"
        },
        "severity": {
          "type": "string",
          "description": "Filter by severity level (info, warning, error, critical)"
        },
        "search": {
          "type": "string",
          "description": "Search guidelines by name or description"
        },
        "take": {
          "type": "number",
          "description": "Maximum number of guidelines to return (default 100)"
        }
      },
      "required": []
    }
  },
  "get_cve_resolution_status": {
    "name": "get_cve_resolution_status",
    "description": "Query CVE resolution status across repositories. Returns CVEResolution entities showing how each CVE was resolved (VEX attestation, auto-fix PR, or human escalation) along with the current status and resolution artifacts.",
    "parameters": {
      "type": "object",
      "properties": {
        "cveId": {
          "type": "string",
          "description": "CVE identifier to look up (e.g., \"CVE-2020-8203\"). Returns all resolutions for this CVE across repositories."
        },
        "repository": {
          "type": "string",
          "description": "Optional repository filter (e.g., \"acme/order-service\"). If provided, returns only the resolution for that specific repository."
        },
        "status": {
          "type": "string",
          "description": "Optional status filter (resolved, fix_pending_review, fix_merged, escalated, failed)"
        },
        "limit": {
          "type": "number",
          "description": "Maximum number of results to return (default 25)"
        }
      },
      "required": [
        "cveId"
      ]
    }
  },
  "get_author_expertise": {
    "name": "get_author_expertise",
    "description": "Get the expertise areas for a specific author or list all experts in the codebase. Shows which parts of the codebase each author knows best based on their commit history. Run the git-insights-analyzer agent first to populate this data.",
    "parameters": {
      "type": "object",
      "properties": {
        "author": {
          "type": "string",
          "description": "Author name to look up (leave empty to get all experts)"
        },
        "repository": {
          "type": "string",
          "description": "Optional repository name to filter by"
        },
        "limit": {
          "type": "number",
          "description": "Maximum number of results to return (default 20)"
        }
      },
      "required": []
    }
  },
  "get_codebase_hotspots": {
    "name": "get_codebase_hotspots",
    "description": "Get the hotspots in the codebase - files that change most frequently. Hotspots often indicate areas of high activity, potential complexity, or code that may need refactoring. Includes churn score, commit count, and risk assessment. Run the git-insights-analyzer agent first to populate this data.",
    "parameters": {
      "type": "object",
      "properties": {
        "repository": {
          "type": "string",
          "description": "Optional repository name to filter by"
        },
        "limit": {
          "type": "number",
          "description": "Maximum number of hotspots to return (default 20)"
        }
      },
      "required": []
    }
  },
  "get_coupling_issues": {
    "name": "get_coupling_issues",
    "description": "Get coupling issues between files or modules in the codebase. Identifies files that frequently change together but belong to different modules, which may indicate hidden dependencies or architectural issues. Run the git-insights-analyzer agent first to populate this data.",
    "parameters": {
      "type": "object",
      "properties": {
        "repository": {
          "type": "string",
          "description": "Optional repository name to filter by"
        },
        "limit": {
          "type": "number",
          "description": "Maximum number of coupling issues to return (default 20)"
        }
      },
      "required": []
    }
  },
  "get_file_experts": {
    "name": "get_file_experts",
    "description": "Get the experts (most knowledgeable contributors) for a specific file based on git history analysis. Returns authors ranked by their expertise score, which considers commit count, lines authored, and recency of contributions. Run the git-insights-analyzer agent first to populate this data.",
    "parameters": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string",
          "description": "The file path to find experts for (e.g., \"src/core/engine.ts\")"
        },
        "repository": {
          "type": "string",
          "description": "Optional repository name to filter by"
        },
        "limit": {
          "type": "number",
          "description": "Maximum number of experts to return (default 5)"
        }
      },
      "required": [
        "filePath"
      ]
    }
  },
  "get_file_risk": {
    "name": "get_file_risk",
    "description": "Get the risk assessment for a specific file based on git history analysis. Risk score considers churn rate, bug fix ratio, number of authors, and recency. Returns risk score (0-1) and a recommendation (low/medium/high/critical). Run the git-insights-analyzer agent first to populate this data.",
    "parameters": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string",
          "description": "The file path to assess risk for"
        },
        "repository": {
          "type": "string",
          "description": "Optional repository name to filter by"
        }
      },
      "required": [
        "filePath"
      ]
    }
  },
  "get_git_insights_summary": {
    "name": "get_git_insights_summary",
    "description": "Get a summary of the git insights analysis for a repository. Includes overall statistics like total commits analyzed, number of files, authors, top hotspots, and health score. Run the git-insights-analyzer agent first to populate this data.",
    "parameters": {
      "type": "object",
      "properties": {
        "repository": {
          "type": "string",
          "description": "Repository name to get summary for (optional)"
        }
      },
      "required": []
    }
  },
  "get_module_boundaries": {
    "name": "get_module_boundaries",
    "description": "Get the logical module boundaries in the codebase based on co-change patterns. Identifies which directories form cohesive modules and where there might be coupling issues between modules. Useful for understanding the actual (not just intended) architecture. Run the git-insights-analyzer agent first to populate this data.",
    "parameters": {
      "type": "object",
      "properties": {
        "repository": {
          "type": "string",
          "description": "Optional repository name to filter by"
        },
        "limit": {
          "type": "number",
          "description": "Maximum number of modules to return (default 20)"
        }
      },
      "required": []
    }
  },
  "get_recent_activity": {
    "name": "get_recent_activity",
    "description": "Get recent activity in the codebase including recently modified files and active directories. Useful for understanding what's currently being worked on and where development effort is focused. Run the git-insights-analyzer agent first to populate this data.",
    "parameters": {
      "type": "object",
      "properties": {
        "repository": {
          "type": "string",
          "description": "Optional repository name to filter by"
        },
        "path": {
          "type": "string",
          "description": "Optional path prefix to filter activity (e.g., \"src/api\")"
        },
        "limit": {
          "type": "number",
          "description": "Maximum number of results to return (default 20)"
        }
      },
      "required": []
    }
  },
  "get_related_files": {
    "name": "get_related_files",
    "description": "Get files that frequently change together with the specified file based on git history co-change analysis. Useful for understanding ripple effects - \"if I change X, what else might need to change?\" Run the git-insights-analyzer agent first to populate this data.",
    "parameters": {
      "type": "object",
      "properties": {
        "filePath": {
          "type": "string",
          "description": "The file path to find related files for"
        },
        "repository": {
          "type": "string",
          "description": "Optional repository name to filter by"
        },
        "limit": {
          "type": "number",
          "description": "Maximum number of related files to return (default 10)"
        }
      },
      "required": [
        "filePath"
      ]
    }
  },
  "semantic_find_referencing_symbols": {
    "name": "semantic_find_referencing_symbols",
    "description": "Find symbols that reference a given symbol. Useful for finding all usages of a class, function, method, or variable across the codebase using semantic analysis. Requires a connected git repository data source.",
    "parameters": {
      "type": "object",
      "properties": {
        "project_id": {
          "type": "string",
          "description": "The project/data source ID to search in (UUID of the connected git repository)"
        },
        "name_path_pattern": {
          "type": "string",
          "description": "Symbol name to find references for (e.g., \"UserService\", \"processData\")"
        },
        "depth": {
          "type": "number",
          "description": "Search depth for nested references"
        },
        "include_body": {
          "type": "boolean",
          "description": "Include the body of referencing symbols in results"
        }
      },
      "required": [
        "project_id",
        "name_path_pattern"
      ]
    }
  },
  "semantic_find_symbol": {
    "name": "semantic_find_symbol",
    "description": "Global symbol search with filtering by kind, depth, and substring matching. Find classes, functions, methods, variables, and other symbols in a codebase using semantic analysis. Requires a connected git repository data source.",
    "parameters": {
      "type": "object",
      "properties": {
        "project_id": {
          "type": "string",
          "description": "The project/data source ID to search in (UUID of the connected git repository)"
        },
        "name_path_pattern": {
          "type": "string",
          "description": "Symbol name or pattern to search for (e.g., \"UserService\", \"handleRequest\")"
        },
        "depth": {
          "type": "number",
          "description": "Search depth (0 for shallow, higher for deeper nested symbols)"
        },
        "relative_path": {
          "type": "string",
          "description": "Restrict search to a specific file path"
        },
        "include_body": {
          "type": "boolean",
          "description": "Include the symbol body/implementation in results"
        },
        "substring_matching": {
          "type": "boolean",
          "description": "Enable substring matching for the symbol name"
        }
      },
      "required": [
        "project_id",
        "name_path_pattern"
      ]
    }
  },
  "semantic_get_symbols_overview": {
    "name": "semantic_get_symbols_overview",
    "description": "Get a high-level understanding of code symbols in a file. Returns an overview of classes, functions, methods, and other symbols defined in the file. Requires a connected git repository data source.",
    "parameters": {
      "type": "object",
      "properties": {
        "project_id": {
          "type": "string",
          "description": "The project/data source ID (UUID of the connected git repository)"
        },
        "relative_path": {
          "type": "string",
          "description": "Path to the file relative to project root (e.g., \"src/services/user.ts\")"
        }
      },
      "required": [
        "project_id",
        "relative_path"
      ]
    }
  },
  "semantic_insert_after_symbol": {
    "name": "semantic_insert_after_symbol",
    "description": "Insert content after a symbol definition. Uses semantic analysis to find the exact end of a symbol and insert new code. Useful for adding new methods to a class or functions after existing ones. Requires a connected git repository.",
    "parameters": {
      "type": "object",
      "properties": {
        "project_id": {
          "type": "string",
          "description": "The project/data source ID (UUID of the connected git repository)"
        },
        "name_path_pattern": {
          "type": "string",
          "description": "Symbol name to insert after (e.g., \"UserService.handleRequest\")"
        },
        "content": {
          "type": "string",
          "description": "Content to insert after the symbol"
        },
        "relative_path": {
          "type": "string",
          "description": "File path if symbol name is ambiguous across files"
        }
      },
      "required": [
        "project_id",
        "name_path_pattern",
        "content"
      ]
    }
  },
  "semantic_list_dir": {
    "name": "semantic_list_dir",
    "description": "List directory contents in a connected git repository. Use this to explore the file structure of git-connected data sources. Requires a connected git repository data source.",
    "parameters": {
      "type": "object",
      "properties": {
        "project_id": {
          "type": "string",
          "description": "The project/data source ID (UUID of the connected git repository)"
        },
        "relative_path": {
          "type": "string",
          "description": "Directory path relative to project root (e.g., \"src/services\")"
        },
        "recursive": {
          "type": "boolean",
          "description": "List recursively (default false)"
        },
        "max_depth": {
          "type": "number",
          "description": "Maximum recursion depth when recursive is true"
        }
      },
      "required": [
        "project_id",
        "relative_path"
      ]
    }
  },
  "semantic_read_file": {
    "name": "semantic_read_file",
    "description": "Read file contents from a connected git repository with optional line range. Use this to view source code from git-connected data sources. Requires a connected git repository data source.",
    "parameters": {
      "type": "object",
      "properties": {
        "project_id": {
          "type": "string",
          "description": "The project/data source ID (UUID of the connected git repository)"
        },
        "relative_path": {
          "type": "string",
          "description": "Path to the file relative to project root (e.g., \"src/index.ts\")"
        },
        "start_line": {
          "type": "number",
          "description": "Start line number (1-indexed)"
        },
        "end_line": {
          "type": "number",
          "description": "End line number (1-indexed)"
        }
      },
      "required": [
        "project_id",
        "relative_path"
      ]
    }
  },
  "semantic_replace_symbol_body": {
    "name": "semantic_replace_symbol_body",
    "description": "Replace the full definition/body of a symbol (class, function, method). Uses semantic analysis to find the exact symbol boundaries and replace the entire implementation. Requires a connected git repository data source.",
    "parameters": {
      "type": "object",
      "properties": {
        "project_id": {
          "type": "string",
          "description": "The project/data source ID (UUID of the connected git repository)"
        },
        "name_path_pattern": {
          "type": "string",
          "description": "Symbol name to replace (e.g., \"UserService.handleRequest\")"
        },
        "new_body": {
          "type": "string",
          "description": "The new body/implementation for the symbol"
        },
        "relative_path": {
          "type": "string",
          "description": "File path if symbol name is ambiguous across files"
        }
      },
      "required": [
        "project_id",
        "name_path_pattern",
        "new_body"
      ]
    }
  },
  "semantic_search_for_pattern": {
    "name": "semantic_search_for_pattern",
    "description": "Search files for regex patterns within a connected git repository. Use this to find specific code patterns, TODO comments, or text across the codebase. Requires a connected git repository data source.",
    "parameters": {
      "type": "object",
      "properties": {
        "project_id": {
          "type": "string",
          "description": "The project/data source ID (UUID of the connected git repository)"
        },
        "pattern": {
          "type": "string",
          "description": "Regex pattern to search for"
        },
        "file_mask": {
          "type": "string",
          "description": "Glob pattern for files to search (e.g., \"*.ts\", \"**/*.py\")"
        },
        "relative_path": {
          "type": "string",
          "description": "Directory to search in (relative to project root)"
        },
        "case_sensitive": {
          "type": "boolean",
          "description": "Case-sensitive search (default true)"
        }
      },
      "required": [
        "project_id",
        "pattern"
      ]
    }
  },
  "query_entities": {
    "name": "query_entities",
    "description": "Query the knowledge graph for entities and their relationships using Cypher. Returns entities matching the specified type and optional filters.",
    "parameters": {
      "type": "object",
      "properties": {
        "entityType": {
          "type": "string",
          "description": "The type of entity to query (e.g., \"Service\", \"CodePattern\", \"Library\")"
        },
        "namePattern": {
          "type": "string",
          "description": "Optional pattern to match entity names (supports wildcards with *)"
        },
        "limit": {
          "type": "number",
          "description": "Maximum number of results to return (default 25)"
        }
      },
      "required": [
        "entityType"
      ]
    }
  },
  "query_symbols": {
    "name": "query_symbols",
    "description": "Query extracted code symbols from repositories. Returns interfaces, structs, classes, functions, and their fields/properties extracted via LSP analysis. Use this to discover configuration APIs, type definitions, and code structure across multiple repositories in a workspace.",
    "parameters": {
      "type": "object",
      "properties": {
        "dataSourceIds": {
          "type": "string",
          "description": "List of data source IDs to query. If not provided, queries all data sources accessible to the tenant. Get data source IDs from the workspace configuration."
        },
        "kinds": {
          "type": "string",
          "description": "Symbol kinds to include. Options: interface, struct, class, enum, type, function, method, property, field. Default returns all kinds."
        },
        "namePattern": {
          "type": "string",
          "description": "Pattern to filter symbol names. Supports wildcards with *. Examples: \"*Options*\" matches all options interfaces, \"Rate*\" matches symbols starting with Rate."
        },
        "includeChildren": {
          "type": "boolean",
          "description": "If true, include nested symbols (fields, methods inside classes/interfaces). If false (default), only return top-level symbols."
        },
        "limit": {
          "type": "number",
          "description": "Maximum number of results to return (default 100, max 1000)"
        }
      },
      "required": []
    }
  },
  "search_knowledge": {
    "name": "search_knowledge",
    "description": "Search the knowledge graph for entities and information using semantic similarity. Use this tool to find relevant context, code patterns, documentation, or any other knowledge stored in the system.",
    "parameters": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string",
          "description": "The search query - describe what you're looking for"
        },
        "entityTypes": {
          "type": "string",
          "description": "Comma-separated list of entity types to filter by (e.g., \"CodePattern,Documentation\")"
        },
        "limit": {
          "type": "number",
          "description": "Maximum number of results to return (default 10)"
        },
        "minSimilarity": {
          "type": "number",
          "description": "Minimum similarity score (0-1) for results (default 0.7)"
        }
      },
      "required": [
        "query"
      ]
    }
  },
  "search_skills": {
    "name": "search_skills",
    "description": "Search for organizational skills learned from past successful agent runs. Skills are reusable patterns that describe how to accomplish specific tasks. Use this tool at the start of a task to find relevant approaches and best practices that have worked for similar tasks in the past.",
    "parameters": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string",
          "description": "Describe what you're trying to accomplish"
        },
        "limit": {
          "type": "number",
          "description": "Maximum number of skills to return (default 5)"
        },
        "minSuccessRate": {
          "type": "number",
          "description": "Minimum success rate filter 0-1 (default 0.3)"
        },
        "status": {
          "type": "string",
          "description": "Comma-separated list of status filters (approved, candidate, deprecated). Default \"approved,candidate\""
        }
      },
      "required": [
        "query"
      ]
    }
  },
  "get_agent_run_output": {
    "name": "get_agent_run_output",
    "description": "Get the output and entities produced by a completed agent run. Returns the raw output from the agent and optionally the entities it created in the knowledge graph.\nUse this after an agent run completes (status: completed) to retrieve its results.",
    "parameters": {
      "type": "object",
      "properties": {
        "agentRunId": {
          "type": "string",
          "description": "The agent run ID returned by invoke_agent."
        },
        "includeEntities": {
          "type": "boolean",
          "description": "If true, also return the entities created by this agent run. Defaults to false."
        }
      },
      "required": [
        "agentRunId"
      ]
    }
  },
  "get_agent_run_status": {
    "name": "get_agent_run_status",
    "description": "Get the current status of an agent run. Returns status (queued, running, completed, failed, cancelled), timestamps, and any error information.\nUse this to monitor the progress of an agent started with invoke_agent.",
    "parameters": {
      "type": "object",
      "properties": {
        "agentRunId": {
          "type": "string",
          "description": "The agent run ID returned by invoke_agent."
        }
      },
      "required": [
        "agentRunId"
      ]
    }
  },
  "invoke_agent": {
    "name": "invoke_agent",
    "description": "Trigger an agent run by name or ID. The agent will execute asynchronously and you can check its progress using get_agent_run_status.\nUse this to run discovery agents (flow-discovery-agent, service-discovery-agent, etc.) or any other agent defined in the system.\nReturns an agentRunId that can be used to track progress and retrieve results.",
    "parameters": {
      "type": "object",
      "properties": {
        "agentKindName": {
          "type": "string",
          "description": "Name of the agent to run (e.g., \"flow-discovery-agent\", \"service-discovery-agent\"). Either agentKindName or agentKindId is required."
        },
        "agentKindId": {
          "type": "string",
          "description": "UUID of the agent to run. Either agentKindName or agentKindId is required."
        },
        "dataSourceId": {
          "type": "string",
          "description": "Optional data source (repository) ID to analyze. Required for most discovery agents."
        },
        "workspaceId": {
          "type": "string",
          "description": "Optional workspace ID for scoping the agent's output."
        },
        "input": {
          "type": "string",
          "description": "Additional input parameters for the agent, as defined in its inputSchema. These are agent-specific parameters (e.g., batchSize, repositoryPath)."
        }
      },
      "required": []
    }
  },
  "list_agent_kinds": {
    "name": "list_agent_kinds",
    "description": "List all available agents (agent types) in the system. Returns agent metadata including name, description, input schema, and status.\nUse this to discover what agents are available before invoking them with invoke_agent.",
    "parameters": {
      "type": "object",
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "Filter by enabled status. If true, only return enabled agents. If false, only return disabled agents. If not provided, return all."
        },
        "interactive": {
          "type": "boolean",
          "description": "Filter by interactive mode. If true, only return interactive agents. If false, only return non-interactive agents."
        }
      },
      "required": []
    }
  },
  "get_runbook": {
    "name": "get_runbook",
    "description": "Get a runbook for a specific service or operational scenario. Returns investigation steps, mitigation commands, escalation paths, and related context.\nPREFER: Use 'incident_response' during incidents - it includes the runbook plus escalation contacts, similar incidents, and ownership in one call.\nUSE THIS WHEN: You need just the runbook steps without the incident context, or when referencing runbooks outside of an active incident.",
    "parameters": {
      "type": "object",
      "properties": {
        "service": {
          "type": "string",
          "description": "Service name or scenario (e.g., \"order-service\", \"checkout-failures\")."
        }
      },
      "required": [
        "service"
      ]
    }
  },
  "get_security_patterns": {
    "name": "get_security_patterns",
    "description": "Get security patterns and anti-patterns learned from past incidents. Returns code examples of what NOT to do and the correct approach.\nPREFER: Use 'incident_response' during active incidents - includes relevant security patterns plus runbooks, escalation contacts, and similar incidents.\nUSE THIS WHEN: You need to browse security patterns for code review or education, without the context of a specific incident or service.",
    "parameters": {
      "type": "object",
      "properties": {
        "category": {
          "type": "string",
          "description": "Security category to search for (e.g., \"rate-limiting\", \"authentication\", \"injection\", \"authorization\"). Leave empty to get all patterns."
        },
        "query": {
          "type": "string",
          "description": "Search query - can be a topic or keyword (e.g., \"X-Forwarded-For\", \"JWT\")."
        }
      },
      "required": []
    }
  },
  "search_adrs": {
    "name": "search_adrs",
    "description": "Search Architecture Decision Records (ADRs) by keyword or topic. Finds ADRs that match the query in their title, decision, rationale, or alternatives. Use this to understand why architectural decisions were made or to find relevant context for new decisions.\nPREFER: Use 'code_migration' when researching ADRs for a specific migration. Use 'understand_flow' when researching ADRs for a specific business flow.\nUSE THIS WHEN: You need to browse ADRs by topic without specific migration or flow context, or when researching architectural patterns broadly.",
    "parameters": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string",
          "description": "Search query - can be a keyword (e.g., \"RabbitMQ\"), topic (e.g., \"messaging\"), or question (e.g., \"why message queue\"). Searches title, decision, and rationale."
        },
        "status": {
          "type": "string",
          "description": "Filter by ADR status: \"accepted\", \"proposed\", \"deprecated\", \"superseded\". Leave empty to search all statuses."
        }
      },
      "required": [
        "query"
      ]
    }
  },
  "search_flows": {
    "name": "search_flows",
    "description": "Search for documented business flows and workflows by keyword or service. Returns flow details including steps, services involved, and related context.\nPREFER: Use 'understand_flow' for complete flow analysis including ownership, runbooks, past incidents, and architectural decisions in one call.\nUSE THIS WHEN: You need to search across multiple flows by keyword, or when browsing available flows without full operational context.",
    "parameters": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string",
          "description": "Search query - can be a flow name (e.g., \"checkout\"), feature (e.g., \"payment\"), or service (e.g., \"order-service\"). Searches title, purpose, and steps."
        }
      },
      "required": [
        "query"
      ]
    }
  },
  "search_incidents": {
    "name": "search_incidents",
    "description": "Search past incidents by keyword, service, or symptom. Returns incidents with root causes, lessons learned, and affected services.\nPREFER: Use 'incident_response' during active incidents - it provides escalation contacts, runbooks, and similar incidents for a specific service.\nUSE THIS WHEN: You need to search across all incidents by keyword/symptom, or when researching historical patterns not tied to a specific service.",
    "parameters": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string",
          "description": "Search query - can be a keyword (e.g., \"timeout\"), service (e.g., \"checkout\"), or symptom (e.g., \"5xx errors\"). Searches title, root cause, and symptoms."
        },
        "severity": {
          "type": "string",
          "description": "Filter by severity: \"SEV-1\", \"SEV-2\", \"SEV-3\", \"High\", \"Medium\", \"Low\". Leave empty to search all severities."
        },
        "service": {
          "type": "string",
          "description": "Filter to incidents affecting a specific service."
        }
      },
      "required": [
        "query"
      ]
    }
  },
  "get_change_confidence": {
    "name": "get_change_confidence",
    "description": "Calculate a confidence score for a proposed code change.\nAggregates multiple risk signals into a single 0-100 score: - File risk metrics (churn, bug-fix ratio, hotspot status) - Blast radius (direct/transitive dependents, critical services) - Historical incidents (past issues in affected areas) - Code coupling (files that change together) - Expert coverage (knowledge concentration risk)\nScore interpretation: - 90-100 (GREEN): Low risk, safe to merge - 75-89 (YELLOW): Moderate risk, review carefully - 50-74 (ORANGE): High risk, extensive testing needed - 0-49 (RED): Critical risk, escalation recommended\nUSE THIS BEFORE: Merging PRs, committing significant changes, deployments\nExample: get_change_confidence(files=[\"src/payment/processor.ts\", \"src/payment/api.ts\"])",
    "parameters": {
      "type": "object",
      "properties": {
        "files": {
          "type": "string",
          "description": "List of file paths being changed (relative to repository root). Example: [\"src/payment/processor.ts\", \"src/checkout/cart.ts\"]"
        },
        "repository": {
          "type": "string",
          "description": "Repository name for scoping queries. Optional but recommended for accuracy."
        },
        "changeType": {
          "type": "string",
          "description": "Type of change: \"feature\", \"bugfix\", \"refactor\", \"dependency\", \"config\". Affects scoring weights. Default: auto-detect from files."
        }
      },
      "required": [
        "files"
      ]
    }
  },
  "get_file_risk_factors": {
    "name": "get_file_risk_factors",
    "description": "Get detailed risk analysis for a single file.\nReturns all risk signals contributing to the file's risk score: - Churn metrics (change frequency, commit count) - Bug-fix ratio (% of commits that are fixes) - Author analysis (expert count, knowledge concentration) - Historical incidents linked to this file/service - Coupling analysis (files that change together)\nUSE THIS WHEN: You want detailed breakdown for a specific file rather than an aggregate score for multiple files.",
    "parameters": {
      "type": "object",
      "properties": {
        "filepath": {
          "type": "string",
          "description": "File path to analyze (relative to repository root)."
        },
        "repository": {
          "type": "string",
          "description": "Repository name for scoping. Optional."
        }
      },
      "required": [
        "filepath"
      ]
    }
  },
  "blast_radius": {
    "name": "blast_radius",
    "description": "Analyze the full impact of changes to a service or package. Shows all dependents (direct and transitive), affected business flows, teams to notify, and historical incidents caused by similar changes. Use BEFORE making changes to understand the risk.\nThis composite skill replaces the need to call: get-transitive-dependents, get-service-flows, get-service-dependents, and get-team-services individually.",
    "parameters": {
      "type": "object",
      "properties": {
        "target": {
          "type": "string",
          "description": "Service or package name to analyze (e.g., \"auth-service\", \"@acme/logging\", \"payment-api\"). Supports partial matching."
        },
        "changeType": {
          "type": "string",
          "description": "Type of change being planned: \"breaking\", \"deprecation\", \"upgrade\", \"refactor\", \"feature\". Helps prioritize risk assessment."
        }
      },
      "required": [
        "target"
      ]
    }
  },
  "code_migration": {
    "name": "code_migration",
    "description": "Complete migration guidance for moving from one package/pattern to another. Shows current migration status, examples from teams that have completed it, equivalent field mappings, and services still pending migration. Use when planning or executing a migration.\nThis composite skill replaces the need to call: get-migration-status, get-migration-examples, find-equivalent-fields, get-adoption-status, and get-transitive-dependents individually.",
    "parameters": {
      "type": "object",
      "properties": {
        "fromPackage": {
          "type": "string",
          "description": "Package or pattern to migrate FROM (e.g., \"moment\", \"axios\", \"log4js\", \"@old/auth\"). Supports partial matching."
        },
        "toPackage": {
          "type": "string",
          "description": "Package or pattern to migrate TO (e.g., \"date-fns\", \"@acme/http-client\", \"@acme/logging\"). If omitted, will suggest alternatives."
        }
      },
      "required": [
        "fromPackage"
      ]
    }
  },
  "dependency_check": {
    "name": "dependency_check",
    "description": "Complete health check for a package dependency. Shows vulnerabilities, upgrade history (did past upgrades require code changes?), migration examples from other teams, and recommended internal alternatives. Use BEFORE adding or upgrading any dependency.\nThis composite skill replaces the need to call: check-dependency-health, get-package-upgrade-history, get-migration-examples, get-recommended-packages, and get-package-vulnerabilities individually.",
    "parameters": {
      "type": "object",
      "properties": {
        "packageName": {
          "type": "string",
          "description": "Package name to check (e.g., \"lodash\", \"@acme/http-client\", \"axios\", \"moment\"). Supports partial matching."
        },
        "ecosystem": {
          "type": "string",
          "description": "Package ecosystem: \"npm\", \"maven\", \"pypi\", \"go\". Leave empty to search all ecosystems."
        }
      },
      "required": [
        "packageName"
      ]
    }
  },
  "incident_response": {
    "name": "incident_response",
    "description": "Everything you need during an incident: escalation contacts, runbooks, similar past incidents with their root causes, and service ownership. Use this IMMEDIATELY when there's a production issue or incident.\nThis composite skill replaces the need to call: get-incident-contacts, get-runbook, search-incidents, get-service-ownership, and get-security-patterns individually.",
    "parameters": {
      "type": "object",
      "properties": {
        "service": {
          "type": "string",
          "description": "Service or feature experiencing the issue (e.g., \"checkout\", \"payment-service\", \"order-api\"). Supports partial matching."
        },
        "symptom": {
          "type": "string",
          "description": "Description of the issue/symptom (e.g., \"5xx errors\", \"high latency\", \"connection timeouts\"). Used to find similar past incidents."
        }
      },
      "required": [
        "service"
      ]
    }
  },
  "investigate_service": {
    "name": "investigate_service",
    "description": "Comprehensive service investigation that returns everything you need to understand a service: its details, dependencies, dependents, ownership, documentation, business flows, active Jira issues, and GitLab issues. Use this as your FIRST tool when investigating, debugging, or understanding any service.\nThis composite skill replaces the need to call: get-service, get-service-dependencies, get-service-dependents, get-service-ownership, get-service-documentation, get-service-flows, and search_gitlab_issues individually.",
    "parameters": {
      "type": "object",
      "properties": {
        "serviceName": {
          "type": "string",
          "description": "Service name to investigate (e.g., \"order-service\", \"payment-api\"). Supports partial matching."
        }
      },
      "required": [
        "serviceName"
      ]
    }
  },
  "understand_flow": {
    "name": "understand_flow",
    "description": "Deep dive into a business flow or process. Shows the complete step-by-step flow, all services involved with their roles, related architectural decisions, past incidents, and operational runbooks. Use this to understand how a feature or business process works end-to-end.\nThis composite skill replaces the need to call: get-flow, get-flow-services, search-adrs, search-incidents, and get-runbook individually.",
    "parameters": {
      "type": "object",
      "properties": {
        "flowName": {
          "type": "string",
          "description": "Flow or process name (e.g., \"checkout\", \"payment-processing\", \"order-fulfillment\", \"user-registration\"). Supports partial matching."
        }
      },
      "required": [
        "flowName"
      ]
    }
  },
  "add_confluence_comment": {
    "name": "add_confluence_comment",
    "description": "Add a footer comment to a Confluence page. Requires a connected and enabled Confluence data source. Comments are visible at the bottom of the page.",
    "parameters": {
      "type": "object",
      "properties": {
        "pageId": {
          "type": "string",
          "description": "The ID of the Confluence page to comment on."
        },
        "body": {
          "type": "string",
          "description": "The comment content in Confluence storage format (XHTML). For example: \"<p>This looks good!</p>\"."
        }
      },
      "required": [
        "pageId",
        "body"
      ]
    }
  },
  "create_confluence_page": {
    "name": "create_confluence_page",
    "description": "Create a new page in a Confluence space. Requires a connected and enabled Confluence data source. The body should be in Confluence storage format (XHTML). Returns the created page ID, title, and URL.",
    "parameters": {
      "type": "object",
      "properties": {
        "spaceId": {
          "type": "string",
          "description": "The Confluence space ID to create the page in. This is the space's internal ID (not the human-readable key). Use search_confluence_pages with CQL 'type = \"space\"' to find space IDs."
        },
        "title": {
          "type": "string",
          "description": "The title of the new page."
        },
        "body": {
          "type": "string",
          "description": "The page content in Confluence storage format (XHTML). For example: \"<p>Hello world</p>\" or \"<h1>Title</h1><p>Content</p>\"."
        },
        "parentId": {
          "type": "string",
          "description": "Optional parent page ID. If provided, the new page will be created as a child of this page."
        },
        "status": {
          "type": "string",
          "description": "Page status. Defaults to \"current\" (published). Use \"draft\" for unpublished pages."
        }
      },
      "required": [
        "spaceId",
        "title",
        "body"
      ]
    }
  },
  "get_confluence_page": {
    "name": "get_confluence_page",
    "description": "Retrieve a Confluence page's content and metadata. Use this to get page context before updating, including the current version number needed for updates. Requires a connected and enabled Confluence data source.",
    "parameters": {
      "type": "object",
      "properties": {
        "pageId": {
          "type": "string",
          "description": "The ID of the Confluence page to retrieve."
        }
      },
      "required": [
        "pageId"
      ]
    }
  },
  "search_confluence_pages": {
    "name": "search_confluence_pages",
    "description": "Search for Confluence pages using CQL (Confluence Query Language). Requires a connected and enabled Confluence data source. Example queries: 'title = \"Meeting Notes\"', 'space = \"ENG\" AND type = \"page\"', 'text ~ \"deployment\"'.",
    "parameters": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string",
          "description": "CQL query string. Examples: 'title = \"Page Title\"', 'space = \"ENG\" AND text ~ \"search term\"', 'label = \"runbook\"'."
        },
        "limit": {
          "type": "number",
          "description": "Maximum number of results to return. Defaults to 10."
        }
      },
      "required": [
        "query"
      ]
    }
  },
  "update_confluence_page": {
    "name": "update_confluence_page",
    "description": "Update an existing Confluence page's content. Requires a connected and enabled Confluence data source. You must provide the current version number (incremented by 1). Use get_confluence_page first to retrieve the current version.",
    "parameters": {
      "type": "object",
      "properties": {
        "pageId": {
          "type": "string",
          "description": "The ID of the Confluence page to update."
        },
        "title": {
          "type": "string",
          "description": "The updated title of the page."
        },
        "body": {
          "type": "string",
          "description": "The updated page content in Confluence storage format (XHTML)."
        },
        "version": {
          "type": "number",
          "description": "The new version number. Must be the current version + 1. Use get_confluence_page to retrieve the current version first."
        }
      },
      "required": [
        "pageId",
        "title",
        "body",
        "version"
      ]
    }
  },
  "create_code_module": {
    "name": "create_code_module",
    "description": "Register a CodeModule mapping file patterns to a service or package.\nCodeModules bridge file paths to architectural entities. When a developer edits a file matching a module's patterns, the system can surface relevant ADRs, incidents, and context.\nExample: Create a module for payment processing code: create_code_module(\n  name=\"payment-processor\",\n  filePatterns=[\"src/payment/**\", \"lib/payment-utils/**\"],\n  service=\"payment-service\",\n  criticality=\"critical\",\n  tags=[\"pci-compliant\", \"financial\"]\n)\nNOTE: This tool creates the entity. For bulk import, use the code-module-discovery agent.",
    "parameters": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "Unique name for the code module (e.g., \"payment-processor\", \"auth-middleware\"). Use lowercase with hyphens."
        },
        "description": {
          "type": "string",
          "description": "Brief description of what this module contains and its purpose."
        },
        "filePatterns": {
          "type": "string",
          "description": "Glob patterns that match files belonging to this module. Examples: [\"src/payment/**\"], [\"packages/auth/src/**\", \"packages/auth/lib/**\"]"
        },
        "service": {
          "type": "string",
          "description": "Name of the Service entity this module belongs to. Must match an existing Service entity name."
        },
        "package": {
          "type": "string",
          "description": "Name of the Package entity this module belongs to (for internal packages). Use either service OR package, not both."
        },
        "technology": {
          "type": "string",
          "description": "Primary technology/language: \"typescript\", \"go\", \"java\", \"python\", etc."
        },
        "criticality": {
          "type": "string",
          "description": "Criticality level: \"critical\", \"high\", \"medium\", \"low\". Affects how prominently context is surfaced. Default: \"medium\"."
        },
        "tags": {
          "type": "string",
          "description": "Tags for categorization and pattern matching. Examples: [\"pci-compliant\", \"user-facing\", \"background-job\"]"
        },
        "repository": {
          "type": "string",
          "description": "Repository this module belongs to (for multi-repo setups)."
        }
      },
      "required": [
        "name",
        "description",
        "filePatterns"
      ]
    }
  },
  "get_file_context": {
    "name": "get_file_context",
    "description": "Get decision context for a file path - the key tool for \"Decision Context at Point of Coding\".\nGiven a file path being edited, returns all relevant architectural context: - The CodeModule and Service the file belongs to - ADRs that apply to this service/module - Recent incidents that affected this area - Security patterns relevant to this code - Code experts who know this file best - Blast radius (what depends on this service) - Related runbooks for operational context\nResults are scored by relevance and recency.\nUSE THIS WHEN: A developer or agent is about to modify a file and needs to understand the architectural context, past issues, and potential impact before making changes.\nExample: get_file_context(filepath=\"src/payment/processor.ts\") Returns: ADRs about payment idempotency, past payment incidents, PCI security patterns, experts on the payment module, services that depend on payment-service.",
    "parameters": {
      "type": "object",
      "properties": {
        "filepath": {
          "type": "string",
          "description": "The file path being edited (relative to repository root). Examples: \"src/payment/processor.ts\", \"packages/auth/src/index.ts\""
        },
        "repository": {
          "type": "string",
          "description": "Repository name to scope the search. If omitted, searches across all repositories."
        },
        "includeBlastRadius": {
          "type": "boolean",
          "description": "Include blast radius analysis (services that depend on this one). Default: true. Set to false for faster response when impact analysis isn't needed."
        },
        "incidentLookbackDays": {
          "type": "number",
          "description": "How many days back to look for relevant incidents. Default: 180 (6 months)."
        }
      },
      "required": [
        "filepath"
      ]
    }
  },
  "list_code_modules": {
    "name": "list_code_modules",
    "description": "List all CodeModule entities, optionally filtered by service or repository.\nShows file pattern mappings that enable decision context lookup.",
    "parameters": {
      "type": "object",
      "properties": {
        "service": {
          "type": "string",
          "description": "Filter to modules belonging to a specific service."
        },
        "repository": {
          "type": "string",
          "description": "Filter to modules in a specific repository."
        },
        "limit": {
          "type": "number",
          "description": "Maximum number of modules to return. Default: 50."
        }
      },
      "required": []
    }
  },
  "resolve_file_to_service": {
    "name": "resolve_file_to_service",
    "description": "Quick lookup: resolve a file path to its owning service.\nLighter-weight than get_file_context - just returns the service mapping without all the decision context. Use this for fast service identification.\nFalls back to heuristics if no CodeModule matches: 1. Checks if path contains a known service name 2. Looks for service.json/package.json in parent directories 3. Uses repository name as fallback",
    "parameters": {
      "type": "object",
      "properties": {
        "filepath": {
          "type": "string",
          "description": "File path to resolve (relative to repository root)."
        },
        "repository": {
          "type": "string",
          "description": "Repository name for disambiguation in multi-repo setups."
        }
      },
      "required": [
        "filepath"
      ]
    }
  },
  "create_feature_pr": {
    "name": "create_feature_pr",
    "description": "Update a Feature entity with Pull Request information. Call this after creating a PR via gh CLI.",
    "parameters": {
      "type": "object",
      "properties": {
        "featureName": {
          "type": "string",
          "description": "The feature name to update"
        },
        "prNumber": {
          "type": "number",
          "description": "The PR number"
        },
        "prUrl": {
          "type": "string",
          "description": "The full PR URL"
        },
        "prTitle": {
          "type": "string",
          "description": "The PR title"
        },
        "reviewers": {
          "type": "string",
          "description": "Comma-separated list of reviewer usernames"
        },
        "filesChanged": {
          "type": "string",
          "description": "Comma-separated list of changed files"
        }
      },
      "required": [
        "featureName",
        "prNumber",
        "prUrl",
        "prTitle"
      ]
    }
  },
  "get_feature": {
    "name": "get_feature",
    "description": "Retrieve information about a Feature entity by name.",
    "parameters": {
      "type": "object",
      "properties": {
        "featureName": {
          "type": "string",
          "description": "The feature name to look up"
        }
      },
      "required": [
        "featureName"
      ]
    }
  },
  "merge_feature_pr": {
    "name": "merge_feature_pr",
    "description": "Mark a Feature as merged. Call this after successfully merging the PR.",
    "parameters": {
      "type": "object",
      "properties": {
        "featureName": {
          "type": "string",
          "description": "The feature name to mark as merged"
        },
        "mergeCommit": {
          "type": "string",
          "description": "The merge commit SHA"
        }
      },
      "required": [
        "featureName"
      ]
    }
  },
  "start_new_feature": {
    "name": "start_new_feature",
    "description": "Register a new development feature in the Context Engine. Creates a Feature entity to track the feature lifecycle including branch, worktree, decisions, and PR info. Call this after creating the git worktree and feature branch.",
    "parameters": {
      "type": "object",
      "properties": {
        "featureName": {
          "type": "string",
          "description": "The feature name (used as entity name)"
        },
        "branchName": {
          "type": "string",
          "description": "The full git branch name (e.g., feature/my-feature)"
        },
        "worktreePath": {
          "type": "string",
          "description": "Relative path to the worktree (e.g., .worktrees/my-feature)"
        },
        "baseBranch": {
          "type": "string",
          "description": "The base branch (default main)"
        },
        "description": {
          "type": "string",
          "description": "Brief description of the feature"
        }
      },
      "required": [
        "featureName",
        "branchName",
        "worktreePath"
      ]
    }
  },
  "update_feature_decisions": {
    "name": "update_feature_decisions",
    "description": "Add a design decision to an existing Feature entity. Use this to record architectural choices, implementation decisions, and their rationale.",
    "parameters": {
      "type": "object",
      "properties": {
        "featureName": {
          "type": "string",
          "description": "The feature name to update"
        },
        "decision": {
          "type": "string",
          "description": "The design decision that was made"
        },
        "rationale": {
          "type": "string",
          "description": "The reasoning behind this decision"
        },
        "alternatives": {
          "type": "string",
          "description": "Alternative approaches considered"
        }
      },
      "required": [
        "featureName",
        "decision",
        "rationale"
      ]
    }
  },
  "get_service_documentation": {
    "name": "get_service_documentation",
    "description": "Get all documentation related to a specific service. Returns incidents that affected the service, runbooks for it, flows involving it, ADRs that mention it, and relevant security patterns.\nPREFER: Use 'investigate_service' for comprehensive service analysis including documentation, dependencies, ownership, and architectural context in one call.\nUSE THIS WHEN: You need only the documentation artifacts without the full service context, or when compiling documentation for multiple services.",
    "parameters": {
      "type": "object",
      "properties": {
        "serviceName": {
          "type": "string",
          "description": "Service name to find documentation for (e.g., \"order-service\", \"payment-api\"). Supports partial matching."
        },
        "limit": {
          "type": "number",
          "description": "Maximum number of results per doc type (default 5 each)."
        }
      },
      "required": [
        "serviceName"
      ]
    }
  },
  "list_documentation": {
    "name": "list_documentation",
    "description": "List all indexed documentation with optional filters. Returns incidents, ADRs, runbooks, flows, and security patterns. Use this to browse available documentation or filter by type/severity.",
    "parameters": {
      "type": "object",
      "properties": {
        "docType": {
          "type": "string",
          "description": "Filter by documentation type: \"Incident\", \"ADR\", \"Runbook\", \"Flow\", \"CriticalFlow\", or \"SecurityPattern\". Leave empty for all types."
        },
        "severity": {
          "type": "string",
          "description": "Filter by severity (for incidents and security patterns): \"critical\", \"high\", \"medium\", \"low\", \"SEV-1\", \"SEV-2\", \"SEV-3\"."
        },
        "status": {
          "type": "string",
          "description": "Filter by status (for ADRs): \"Accepted\", \"Proposed\", \"Deprecated\", \"Superseded\"."
        },
        "limit": {
          "type": "number",
          "description": "Maximum number of results to return (default 30)."
        }
      },
      "required": []
    }
  },
  "search_all_documentation": {
    "name": "search_all_documentation",
    "description": "Unified search across all documentation types - incidents, ADRs, runbooks, flows, and security patterns. Returns results from all doc types ranked by relevance. Use this when you need to find any documentation related to a topic.",
    "parameters": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string",
          "description": "Search query - can be a keyword, service name, error message, or topic. Searches across titles, descriptions, and content of all doc types."
        },
        "docTypes": {
          "type": "string",
          "description": "Comma-separated list of doc types to include. Options: \"Incident,ADR,Runbook,Flow,SecurityPattern\". Leave empty to search all types."
        },
        "limit": {
          "type": "number",
          "description": "Maximum number of results to return (default 20)."
        }
      },
      "required": [
        "query"
      ]
    }
  },
  "get_flow_services": {
    "name": "get_flow_services",
    "description": "Get all services involved in a flow with their roles and order. Shows the orchestrator, each service's responsibility, and the sequence of service calls.\nPREFER: Use 'understand_flow' for complete flow analysis including ownership, runbooks, past incidents, and architectural decisions in one call.\nUSE THIS WHEN: You need just the service participation list without the full operational context, or when comparing services across multiple flows.",
    "parameters": {
      "type": "object",
      "properties": {
        "flowName": {
          "type": "string",
          "description": "Flow name to get services for (e.g., \"checkout\", \"payment-processing\"). Supports partial matching."
        }
      },
      "required": [
        "flowName"
      ]
    }
  },
  "get_flow": {
    "name": "get_flow",
    "description": "Get detailed information about a specific flow including all steps, services involved, compensation logic, and related incidents/ADRs.\nPREFER: Use 'understand_flow' for complete flow analysis including ownership, runbooks, past incidents, and architectural decisions in one call.\nUSE THIS WHEN: You need just the flow definition and steps without the full operational context, or when listing multiple flows.",
    "parameters": {
      "type": "object",
      "properties": {
        "flowName": {
          "type": "string",
          "description": "Flow name to look up (e.g., \"checkout\", \"order-processing\"). Supports partial matching."
        }
      },
      "required": [
        "flowName"
      ]
    }
  },
  "get_service_flows": {
    "name": "get_service_flows",
    "description": "Get all flows that involve a specific service. Shows which business processes the service participates in, its role (orchestrator or participant), and the other services it collaborates with.\nPREFER: Use 'investigate_service' for comprehensive service analysis including flows, dependencies, documentation, and ownership in one call. Use 'blast_radius' when assessing impact of changes to this service.\nUSE THIS WHEN: You need just the flow participation list without the full service context, or when mapping flows for multiple services.",
    "parameters": {
      "type": "object",
      "properties": {
        "serviceName": {
          "type": "string",
          "description": "Service name to find flows for (e.g., \"order-service\", \"payment-api\"). Supports partial matching."
        },
        "limit": {
          "type": "number",
          "description": "Maximum number of flows to return (default 15)."
        }
      },
      "required": [
        "serviceName"
      ]
    }
  },
  "list_flows": {
    "name": "list_flows",
    "description": "List all business flows discovered in the codebase. Includes both CriticalFlow (discovered from code, spanning 3+ services) and Flow (documented in markdown) entities. Use this to understand the major business processes in your system.",
    "parameters": {
      "type": "object",
      "properties": {
        "flowType": {
          "type": "string",
          "description": "Filter by flow type: \"CriticalFlow\" (code-discovered) or \"Flow\" (documented). Leave empty for both types."
        },
        "minServices": {
          "type": "number",
          "description": "Minimum number of services a flow must span. Default is 1."
        },
        "repository": {
          "type": "string",
          "description": "Filter by repository name (partial match)."
        },
        "limit": {
          "type": "number",
          "description": "Maximum number of flows to return (default 20)."
        }
      },
      "required": []
    }
  },
  "query_gcp_error_group_stats": {
    "name": "query_gcp_error_group_stats",
    "description": "Query GCP Error Reporting API for error group statistics with time-windowed counts. Returns timedCounts arrays for spike detection. Requires a connected and enabled gcp_monitoring data source.",
    "parameters": {
      "type": "object",
      "properties": {
        "projectId": {
          "type": "string",
          "description": "GCP project ID (e.g., \"my-project-123\")."
        },
        "timeRangePeriod": {
          "type": "string",
          "description": "Predefined time range: PERIOD_1_HOUR, PERIOD_6_HOURS, PERIOD_1_DAY, PERIOD_1_WEEK, PERIOD_30_DAYS. Default: PERIOD_1_HOUR."
        },
        "timedCountDuration": {
          "type": "string",
          "description": "Duration for timed count buckets (e.g., \"900s\" for 15 minutes). Default: 900s."
        },
        "serviceFilter": {
          "type": "string",
          "description": "Filter by service name (e.g., \"my-service\"). Optional."
        },
        "pageSize": {
          "type": "number",
          "description": "Maximum number of error groups to return. Default: 50."
        }
      },
      "required": [
        "projectId"
      ]
    }
  },
  "add_gitlab_mr_comment": {
    "name": "add_gitlab_mr_comment",
    "description": "Add a comment (note) to an existing GitLab merge request. Requires a connected and enabled GitLab data source.",
    "parameters": {
      "type": "object",
      "properties": {
        "projectId": {
          "type": "string",
          "description": "GitLab project ID (numeric) or URL-encoded path."
        },
        "mergeRequestIid": {
          "type": "string",
          "description": "The internal ID (IID) of the merge request within the project."
        },
        "body": {
          "type": "string",
          "description": "Comment body text. Supports GitLab Flavored Markdown."
        }
      },
      "required": [
        "projectId",
        "mergeRequestIid",
        "body"
      ]
    }
  },
  "create_gitlab_merge_request": {
    "name": "create_gitlab_merge_request",
    "description": "Create a new merge request in a GitLab project. Requires a connected and enabled GitLab data source. Returns the created merge request details including IID and web URL.",
    "parameters": {
      "type": "object",
      "properties": {
        "projectId": {
          "type": "string",
          "description": "GitLab project ID (numeric) or URL-encoded path (e.g., \"123\" or \"group%2Fproject\")."
        },
        "sourceBranch": {
          "type": "string",
          "description": "The source branch name for the merge request."
        },
        "targetBranch": {
          "type": "string",
          "description": "The target branch name (e.g., \"main\", \"develop\")."
        },
        "title": {
          "type": "string",
          "description": "Title of the merge request."
        },
        "description": {
          "type": "string",
          "description": "Description body of the merge request. Supports GitLab Flavored Markdown."
        }
      },
      "required": [
        "projectId",
        "sourceBranch",
        "targetBranch",
        "title"
      ]
    }
  },
  "search_gitlab_issues": {
    "name": "search_gitlab_issues",
    "description": "Search GitLab issues in the knowledge graph. Supports filtering by service name, state, and free-text query against title/description. Use this to find open or closed GitLab issues related to a service or topic.\nPrefer investigate_service when you need a full service overview — use this tool when you need a targeted GitLab issue search (e.g. all open issues for a service, issues matching a keyword).",
    "parameters": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string",
          "description": "Free-text search against issue title and description (case-insensitive substring match). Leave empty to return all issues matching other filters."
        },
        "serviceName": {
          "type": "string",
          "description": "Filter to issues linked to a specific service (e.g. \"order-service\"). Matches against the service node name using a partial, case-insensitive match."
        },
        "state": {
          "type": "string",
          "description": "Filter by issue state: \"opened\" (default) or \"closed\". Pass \"all\" to return both."
        }
      },
      "required": []
    }
  },
  "find_entities": {
    "name": "find_entities",
    "description": "Find entities in the knowledge graph using semantic search. Use this as your starting point for graph exploration - it understands natural language queries and finds semantically similar content even without exact keyword matches.\nSearch strategies: - Natural language: \"authentication services\" finds auth-related entities - Conceptual: \"security vulnerabilities\" finds security findings - Filtered: Set entityTypes to narrow results (e.g., [\"Service\", \"CodePattern\"])\nCommon entity types: Service, CodePattern, Finding, CodeExpert, CodeHotspot, CodeCoupling, Feature, ModuleBoundary, Package, Vulnerability\nReturns entity ID, type, name, data, and similarity score. Use returned IDs with traverse_edges to explore relationships or get_entity_by_id for details.",
    "parameters": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string",
          "description": "Natural language search query describing what you're looking for"
        },
        "entityTypes": {
          "type": "string",
          "description": "Filter to specific entity types (e.g., [\"Service\", \"CodePattern\"])"
        },
        "limit": {
          "type": "number",
          "description": "Maximum number of results to return (default 10)"
        },
        "minSimilarity": {
          "type": "number",
          "description": "Minimum similarity threshold 0-1 (default 0.5)"
        }
      },
      "required": [
        "query"
      ]
    }
  },
  "get_entity_by_id": {
    "name": "get_entity_by_id",
    "description": "Get full details of a specific entity by its ID. Use this when you already have an entity ID (e.g., from find_entities or traverse_edges results) and need to see its complete data.\nReturns: id, name, type, data (JSON properties), createdAt, createdBy",
    "parameters": {
      "type": "object",
      "properties": {
        "entityId": {
          "type": "string",
          "description": "The entity ID to retrieve"
        }
      },
      "required": [
        "entityId"
      ]
    }
  },
  "traverse_edges": {
    "name": "traverse_edges",
    "description": "Traverse edges from an entity to discover related entities. Use this after find_entities to explore the graph structure and relationships.\nDirection controls which edges to follow: - \"out\": Edges where this entity is the source (this -> related) - \"in\": Edges where this entity is the target (related -> this) - \"both\": Edges in either direction (default)\nCommon edge types: depends_on, calls, contains, manages, created_together, references, produces, consumes\nExample workflow: 1. find_entities(query=\"payment service\") -> get service ID 2. traverse_edges(entityId=\"...\", direction=\"out\") -> what does it depend on? 3. traverse_edges(entityId=\"...\", direction=\"in\") -> what depends on it? 4. traverse_edges(entityId=\"...\", edgeType=\"calls\") -> what does it call?\nFor multi-hop traversal, call this tool repeatedly with the returned entity IDs.",
    "parameters": {
      "type": "object",
      "properties": {
        "entityId": {
          "type": "string",
          "description": "The ID of the entity to start traversal from"
        },
        "edgeType": {
          "type": "string",
          "description": "Filter by relationship type (e.g., \"depends_on\", \"calls\", \"contains\")"
        },
        "direction": {
          "type": "string",
          "description": "Direction to traverse - \"in\", \"out\", or \"both\" (default \"both\")"
        },
        "limit": {
          "type": "number",
          "description": "Maximum number of related entities to return (default 25)"
        }
      },
      "required": [
        "entityId"
      ]
    }
  },
  "check_dependency_health": {
    "name": "check_dependency_health",
    "description": "Check the security and license health of a package dependency. Returns vulnerabilities from JFrog Xray, license compliance status, blast radius (which services in your organization use this package), and migration recommendations if issues are found.\nPREFER: Use 'dependency_check' for complete health assessment including upgrade history, migration examples, and recommended alternatives in one call.\nUSE THIS WHEN: You need just the security/license status without upgrade history context, or when checking multiple packages in a loop.",
    "parameters": {
      "type": "object",
      "properties": {
        "packageName": {
          "type": "string",
          "description": "Package name to check. Format depends on ecosystem: - npm: \"lodash\" or \"@scope/package\" - maven: \"org.apache.commons:commons-lang3\" - pypi: \"requests\""
        },
        "version": {
          "type": "string",
          "description": "Specific version to check. If omitted, checks latest indexed version."
        },
        "ecosystem": {
          "type": "string",
          "description": "Package ecosystem (npm, maven, pypi)"
        }
      },
      "required": [
        "packageName",
        "ecosystem"
      ]
    }
  },
  "get_adoption_status": {
    "name": "get_adoption_status",
    "description": "Get adoption status for a recommended package or migration. Shows which services have adopted the package vs. still using old alternatives.\nPREFER: Use 'code_migration' for complete migration guidance including adoption status, field mappings, migration examples, and ADR context in one call.\nUSE THIS WHEN: You need just the adoption percentages and service lists, or when tracking progress across multiple packages.",
    "parameters": {
      "type": "object",
      "properties": {
        "packageName": {
          "type": "string",
          "description": "The recommended package to check adoption for (e.g., \"@acme/http-client\")."
        }
      },
      "required": [
        "packageName"
      ]
    }
  },
  "get_cve_blast_radius": {
    "name": "get_cve_blast_radius",
    "description": "Get the complete TRANSITIVE blast radius for a CVE - which packages are directly affected and which services/packages transitively depend on them. Traverses up to 10 levels of dependency chains to find all impacted services.\nReturns: - Affected packages (directly vulnerable) - Affected services with dependency chains showing how they're impacted - Team ownership and contact information - Separation of direct vs. transitive dependencies - Critical tier services highlighted\nExample: If lodash has CVE-2020-8203, finds not only services using lodash directly, but also services using @acme/auth which depends on lodash.\nPREFER: Use 'blast_radius' for broader impact analysis that includes affected flows, historical incidents, and complete team notifications.\nUSE THIS WHEN: You need just the CVE-specific blast radius without the full incident context, or when analyzing multiple CVEs systematically.",
    "parameters": {
      "type": "object",
      "properties": {
        "cveId": {
          "type": "string",
          "description": "CVE identifier (e.g., \"CVE-2020-8203\", \"CVE-2021-44228\")."
        }
      },
      "required": [
        "cveId"
      ]
    }
  },
  "get_migration_examples": {
    "name": "get_migration_examples",
    "description": "Find examples of services that have migrated from one package to another. Shows which services completed the migration, when, and by whom.\nPREFER: Use 'code_migration' for complete migration guidance including field mappings, ADR context, and recommended patterns in one call.\nUSE THIS WHEN: You need just the list of services that completed a migration, or when tracking migration progress across the organization.",
    "parameters": {
      "type": "object",
      "properties": {
        "fromPackage": {
          "type": "string",
          "description": "Package being migrated from (e.g., \"log4js\", \"axios\")."
        },
        "toPackage": {
          "type": "string",
          "description": "Package being migrated to (e.g., \"@acme/logging\", \"@acme/http-client\"). If omitted, shows all services that removed the fromPackage."
        }
      },
      "required": [
        "fromPackage"
      ]
    }
  },
  "get_migration_path": {
    "name": "get_migration_path",
    "description": "Get migration guidance for upgrading or replacing a package. Shows which services depend on the package and suggests alternatives.\nPREFER: Use 'code_migration' for complete migration guidance including field mappings, migration examples, ADR context, and adoption status in one call.\nUSE THIS WHEN: You need just the dependency graph for a package without the full migration context, or when planning migrations for multiple packages.",
    "parameters": {
      "type": "object",
      "properties": {
        "fromPackage": {
          "type": "string",
          "description": "Package name to migrate from (e.g., \"moment\" or \"pac-resolver\")"
        },
        "toPackage": {
          "type": "string",
          "description": "Target package to migrate to. If omitted, shows current dependents only."
        },
        "ecosystem": {
          "type": "string",
          "description": "Package ecosystem (npm, maven, pypi)"
        }
      },
      "required": [
        "fromPackage",
        "ecosystem"
      ]
    }
  },
  "get_migration_status": {
    "name": "get_migration_status",
    "description": "Get the migration status for a deprecated or replaced package. Shows which services have migrated vs. still pending, with team contacts for follow-up.\nPREFER: Use 'code_migration' for complete migration guidance including field mappings, migration examples, and ADR context in one call.\nUSE THIS WHEN: You need just the migration progress numbers without the full migration context, or when tracking multiple migrations.",
    "parameters": {
      "type": "object",
      "properties": {
        "oldPackage": {
          "type": "string",
          "description": "The deprecated/old package being replaced (e.g., \"log4js\", \"axios\")."
        },
        "newPackage": {
          "type": "string",
          "description": "The recommended replacement package (e.g., \"@acme/logging\"). If omitted, will try to find from package metadata."
        }
      },
      "required": [
        "oldPackage"
      ]
    }
  },
  "get_package_usage_patterns": {
    "name": "get_package_usage_patterns",
    "description": "Analyze how a package is used across your organization. Shows which services use the package and usage statistics. Helpful for understanding adoption and identifying usage patterns.",
    "parameters": {
      "type": "object",
      "properties": {
        "packageName": {
          "type": "string",
          "description": "Package name to analyze (e.g., \"@acme/auth-sdk\" or \"lodash\")"
        },
        "ecosystem": {
          "type": "string",
          "description": "Package ecosystem (npm, maven, pypi)"
        }
      },
      "required": [
        "packageName",
        "ecosystem"
      ]
    }
  },
  "get_package_vulnerabilities": {
    "name": "get_package_vulnerabilities",
    "description": "Get all vulnerabilities linked to a package.\nPREFER: Use 'dependency_check' for complete package health assessment including vulnerabilities, license status, upgrade history, and blast radius in one call.\nUSE THIS WHEN: You need just the vulnerability list for a specific package, or when scanning multiple packages systematically.",
    "parameters": {
      "type": "object",
      "properties": {
        "packageName": {
          "type": "string",
          "description": "Package name pattern to search for"
        }
      },
      "required": [
        "packageName"
      ]
    }
  },
  "get_recommended_packages": {
    "name": "get_recommended_packages",
    "description": "Find recommended internal packages for a specific capability or use case. Searches for packages marked as \"recommended\" that match the query. Use this before adding a new dependency to see if there's an internal package you should use instead.\nPREFER: Use 'dependency_check' when evaluating a specific package - includes recommended alternatives, security status, and migration guidance in one call.\nUSE THIS WHEN: You need to discover what internal packages exist for a capability, without evaluating a specific existing package.",
    "parameters": {
      "type": "object",
      "properties": {
        "capability": {
          "type": "string",
          "description": "What you're looking for - can be a capability (e.g., \"HTTP client\"), use case (e.g., \"logging\"), or functionality (e.g., \"authentication\")."
        },
        "ecosystem": {
          "type": "string",
          "description": "Filter by package ecosystem: \"npm\", \"maven\", \"pypi\", \"go\". Leave empty to search all ecosystems."
        }
      },
      "required": [
        "capability"
      ]
    }
  },
  "get_transitive_dependents": {
    "name": "get_transitive_dependents",
    "description": "Get all services and packages that depend on a package, including transitive dependencies. Shows which services would need to be rebuilt or tested if this package changes.\nPREFER: Use 'blast_radius' for complete impact analysis including affected flows, teams to notify, and historical incidents in one call.\nUSE THIS WHEN: You need just the dependency tree without the broader impact context, or when analyzing package-level dependencies only.",
    "parameters": {
      "type": "object",
      "properties": {
        "packageName": {
          "type": "string",
          "description": "Package name to find dependents for (e.g., \"@acme/logging\" or \"lodash\"). Supports partial matching."
        },
        "maxDepth": {
          "type": "number",
          "description": "Maximum depth to traverse for transitive dependencies. Default is 5. Use 1 for direct dependents only."
        },
        "includePackages": {
          "type": "boolean",
          "description": "Include package-to-package dependencies in results. Default is true. Set to false to only show services."
        }
      },
      "required": [
        "packageName"
      ]
    }
  },
  "list_all_packages": {
    "name": "list_all_packages",
    "description": "Diagnostic tool: List all Package entities in the knowledge graph. Returns all indexed packages to verify entity creation worked.",
    "parameters": {
      "type": "object",
      "properties": {
        "limit": {
          "type": "number",
          "description": "Maximum number of results (default 100)"
        }
      },
      "required": []
    }
  },
  "list_all_vulnerabilities": {
    "name": "list_all_vulnerabilities",
    "description": "Diagnostic tool: List all Vulnerability entities in the knowledge graph. Returns all indexed vulnerabilities to verify entity creation worked.",
    "parameters": {
      "type": "object",
      "properties": {
        "limit": {
          "type": "number",
          "description": "Maximum number of results (default 100)"
        }
      },
      "required": []
    }
  },
  "search_internal_packages": {
    "name": "search_internal_packages",
    "description": "Search for internal packages in JFrog Artifactory using semantic matching. Finds packages by name, description, or functionality, and enriches results with usage statistics (how many services use each package) and security data. Use this to discover internal libraries before implementing new functionality.",
    "parameters": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string",
          "description": "Search query - describe what you're looking for (package name, functionality, or natural language description like \"http client with retry\" or \"logging library\")"
        },
        "ecosystem": {
          "type": "string",
          "description": "Filter by package ecosystem (npm, maven, pypi). Leave empty to search all."
        },
        "limit": {
          "type": "number",
          "description": "Maximum number of results to return (default 10, max 50)"
        }
      },
      "required": [
        "query"
      ]
    }
  },
  "test_cypher_tenant": {
    "name": "test_cypher_tenant",
    "description": "Test Cypher with tenantId",
    "parameters": {
      "type": "object",
      "properties": {},
      "required": []
    }
  },
  "test_cypher_type": {
    "name": "test_cypher_type",
    "description": "Test Cypher with type filter",
    "parameters": {
      "type": "object",
      "properties": {},
      "required": []
    }
  },
  "test_cypher": {
    "name": "test_cypher",
    "description": "Simple test of Cypher query execution",
    "parameters": {
      "type": "object",
      "properties": {},
      "required": []
    }
  },
  "add_jira_comment": {
    "name": "add_jira_comment",
    "description": "Add a comment to an existing Jira issue. Requires a connected and enabled Jira data source. The comment body is wrapped in Atlassian Document Format (ADF).",
    "parameters": {
      "type": "object",
      "properties": {
        "issueKey": {
          "type": "string",
          "description": "The Jira issue key to comment on (e.g., \"ACME-247\")."
        },
        "body": {
          "type": "string",
          "description": "Plain-text comment body. Will be wrapped in ADF paragraph format."
        }
      },
      "required": [
        "issueKey",
        "body"
      ]
    }
  },
  "assign_jira_to_sprint": {
    "name": "assign_jira_to_sprint",
    "description": "Move a Jira issue into a sprint. Uses the Jira Agile REST API. Requires the sprint ID and the issue key. Call multiple times to move several issues. Requires a connected and enabled Jira data source.",
    "parameters": {
      "type": "object",
      "properties": {
        "sprintId": {
          "type": "string",
          "description": "The numeric sprint ID (e.g., \"42\"). Can be obtained from the board's sprints endpoint."
        },
        "issueKey": {
          "type": "string",
          "description": "The issue key to move into the sprint (e.g., \"ACME-123\")."
        }
      },
      "required": [
        "sprintId",
        "issueKey"
      ]
    }
  },
  "create_jira_issue": {
    "name": "create_jira_issue",
    "description": "Create a new Jira issue. Requires a connected and enabled Jira data source. Builds an Atlassian Document Format (ADF) body for the description field. Returns the created issue key and self URL. Priority defaults to the project's default; use transition_jira_issue or the Jira UI to set labels, priority, or other fields after creation.",
    "parameters": {
      "type": "object",
      "properties": {
        "projectKey": {
          "type": "string",
          "description": "The Jira project key (e.g., \"ACME\", \"PROJ\")."
        },
        "summary": {
          "type": "string",
          "description": "One-line summary / title of the issue."
        },
        "issueType": {
          "type": "string",
          "description": "Issue type name (e.g., \"Bug\", \"Task\", \"Story\", \"Epic\"). Must match a type configured in the target project."
        },
        "description": {
          "type": "string",
          "description": "Plain-text description of the issue. Will be wrapped in ADF paragraph format. Defaults to empty if omitted."
        }
      },
      "required": [
        "projectKey",
        "summary",
        "issueType"
      ]
    }
  },
  "get_active_sprint": {
    "name": "get_active_sprint",
    "description": "Get the active sprint for a Jira project, including all sprint issues with status, assignees, affected services, and velocity metrics. Use when asking about current sprint status, what the team is working on, or checking sprint health before adding more work.\nShows in-progress vs. to-do breakdown to assess sprint capacity.",
    "parameters": {
      "type": "object",
      "properties": {
        "projectKey": {
          "type": "string",
          "description": "The Jira project key (e.g., ACME). Returns the currently active sprint for this project."
        }
      },
      "required": [
        "projectKey"
      ]
    }
  },
  "get_jira_epic": {
    "name": "get_jira_epic",
    "description": "Get an epic's details including all child issues with status, assignees, affected services, and progress summary. Use when planning work, assessing risk of a feature initiative, or understanding what a multi-story epic involves across teams and services.\nParticularly useful for sprint planning and cross-team coordination — shows which services and teams are touched by the epic's stories.",
    "parameters": {
      "type": "object",
      "properties": {
        "epicKey": {
          "type": "string",
          "description": "The epic key (e.g., ACME-200). Searches both Epic and JiraIssue entity types to find the epic."
        }
      },
      "required": [
        "epicKey"
      ]
    }
  },
  "get_jira_issue_remote_links": {
    "name": "get_jira_issue_remote_links",
    "description": "Get remote links for a Jira issue. Remote links include GitHub/GitLab PRs, branches, and commits linked via platform integrations (e.g., Jira's GitHub or GitLab app). Returns the linked resources with their URLs, titles, and relationship types. Use this to discover PR-Issue links that are not in commit messages or branch names. Requires a connected and enabled Jira data source.",
    "parameters": {
      "type": "object",
      "properties": {
        "issueKey": {
          "type": "string",
          "description": "The Jira issue key to get remote links for (e.g., \"PROJ-123\")."
        }
      },
      "required": [
        "issueKey"
      ]
    }
  },
  "get_jira_issue": {
    "name": "get_jira_issue",
    "description": "Get full details of a Jira issue including status, assignee, linked PRs, affected services, epic, and sprint. Use when a user mentions a Jira issue key (e.g., ACME-247) or when you need to understand what a ticket represents before implementing it.\nReturns the issue's cross-source connections: which services it affects, which PRs resolve it, what epic/sprint it belongs to, and who is assigned.",
    "parameters": {
      "type": "object",
      "properties": {
        "issueKey": {
          "type": "string",
          "description": "The Jira issue key (e.g., ACME-247, PROJ-123). Must be an exact match."
        }
      },
      "required": [
        "issueKey"
      ]
    }
  },
  "transition_jira_issue": {
    "name": "transition_jira_issue",
    "description": "Transition a Jira issue to a new status. Requires the numeric transition ID, which can be obtained from the issue's available transitions (use get_jira_issue to find the issue, then GET /rest/api/3/issue/{issueKey}/transitions). Requires a connected and enabled Jira data source.",
    "parameters": {
      "type": "object",
      "properties": {
        "issueKey": {
          "type": "string",
          "description": "The Jira issue key to transition (e.g., \"ACME-247\")."
        },
        "transitionId": {
          "type": "string",
          "description": "The numeric transition ID (e.g., \"21\", \"31\"). Get available transitions from the Jira issue's transitions endpoint."
        }
      },
      "required": [
        "issueKey",
        "transitionId"
      ]
    }
  },
  "add_linear_comment": {
    "name": "add_linear_comment",
    "description": "Add a comment to an existing Linear issue. Requires a connected and enabled Linear data source. The comment body supports markdown formatting.",
    "parameters": {
      "type": "object",
      "properties": {
        "issueId": {
          "type": "string",
          "description": "The ID of the issue to comment on. This is the Linear internal UUID."
        },
        "body": {
          "type": "string",
          "description": "The comment body in markdown format."
        }
      },
      "required": [
        "issueId",
        "body"
      ]
    }
  },
  "create_linear_issue": {
    "name": "create_linear_issue",
    "description": "Create a new Linear issue. Requires a connected and enabled Linear data source. Creates an issue with a title and team. Use update_linear_issue afterwards to set priority, assignee, labels, or other optional fields. Returns the created issue identifier and URL.",
    "parameters": {
      "type": "object",
      "properties": {
        "title": {
          "type": "string",
          "description": "The title of the issue."
        },
        "teamId": {
          "type": "string",
          "description": "The ID of the team to create the issue in. Use list_linear_teams to find team IDs."
        },
        "description": {
          "type": "string",
          "description": "Markdown description of the issue. Defaults to empty if omitted."
        }
      },
      "required": [
        "title",
        "teamId"
      ]
    }
  },
  "list_linear_issues": {
    "name": "list_linear_issues",
    "description": "List recent Linear issues ordered by last updated. Requires a connected and enabled Linear data source. Returns up to 50 issues across the workspace. Use this to find issue IDs before updating or commenting.",
    "parameters": {
      "type": "object",
      "properties": {},
      "required": []
    }
  },
  "list_linear_teams": {
    "name": "list_linear_teams",
    "description": "List all teams in the Linear workspace. Requires a connected and enabled Linear data source. Returns team IDs, names, and keys. Useful for finding the teamId parameter needed by create_linear_issue.",
    "parameters": {
      "type": "object",
      "properties": {},
      "required": []
    }
  },
  "transition_linear_issue": {
    "name": "transition_linear_issue",
    "description": "Transition a Linear issue to a new workflow state (e.g., \"In Progress\", \"Done\"). Requires a connected and enabled Linear data source. Use list_linear_issues to find issue IDs and inspect current states.",
    "parameters": {
      "type": "object",
      "properties": {
        "issueId": {
          "type": "string",
          "description": "The ID of the issue to transition. This is the Linear internal UUID."
        },
        "stateId": {
          "type": "string",
          "description": "The ID of the target workflow state. Use the Linear UI or API to discover available state IDs for the issue's team."
        }
      },
      "required": [
        "issueId",
        "stateId"
      ]
    }
  },
  "update_linear_issue": {
    "name": "update_linear_issue",
    "description": "Update the title and description of an existing Linear issue. Both fields are always set to the provided values — retrieve the current issue first (via list_linear_issues) to avoid overwriting fields you don't intend to change. Requires a connected and enabled Linear data source. To change status, use transition_linear_issue instead.",
    "parameters": {
      "type": "object",
      "properties": {
        "issueId": {
          "type": "string",
          "description": "The ID of the issue to update. This is the Linear internal UUID, not the human-readable identifier like \"ENG-123\". Use list_linear_issues to find IDs."
        },
        "title": {
          "type": "string",
          "description": "The new title for the issue."
        },
        "description": {
          "type": "string",
          "description": "The new markdown description for the issue. Pass an empty string to clear."
        }
      },
      "required": [
        "issueId",
        "title",
        "description"
      ]
    }
  },
  "acknowledge_opsgenie_alert": {
    "name": "acknowledge_opsgenie_alert",
    "description": "Acknowledge an Opsgenie alert to stop further escalations. Requires a connected and enabled Opsgenie data source.",
    "parameters": {
      "type": "object",
      "properties": {
        "alertId": {
          "type": "string",
          "description": "The Opsgenie alert ID to acknowledge."
        },
        "user": {
          "type": "string",
          "description": "Display name of the user acknowledging the alert."
        },
        "note": {
          "type": "string",
          "description": "Additional note to add while acknowledging the alert."
        }
      },
      "required": [
        "alertId"
      ]
    }
  },
  "add_opsgenie_note": {
    "name": "add_opsgenie_note",
    "description": "Add a note to an existing Opsgenie alert for additional context. Requires a connected and enabled Opsgenie data source. Notes are visible on the alert timeline.",
    "parameters": {
      "type": "object",
      "properties": {
        "alertId": {
          "type": "string",
          "description": "The Opsgenie alert ID to add a note to."
        },
        "note": {
          "type": "string",
          "description": "The note content to add to the alert."
        },
        "user": {
          "type": "string",
          "description": "Display name of the user adding the note."
        }
      },
      "required": [
        "alertId",
        "note"
      ]
    }
  },
  "close_opsgenie_alert": {
    "name": "close_opsgenie_alert",
    "description": "Close an Opsgenie alert, marking it as resolved. Requires a connected and enabled Opsgenie data source.",
    "parameters": {
      "type": "object",
      "properties": {
        "alertId": {
          "type": "string",
          "description": "The Opsgenie alert ID to close."
        },
        "user": {
          "type": "string",
          "description": "Display name of the user closing the alert."
        },
        "note": {
          "type": "string",
          "description": "Additional note to add while closing the alert."
        }
      },
      "required": [
        "alertId"
      ]
    }
  },
  "escalate_opsgenie_alert": {
    "name": "escalate_opsgenie_alert",
    "description": "Escalate an Opsgenie alert to the next escalation level. Requires a connected and enabled Opsgenie data source.",
    "parameters": {
      "type": "object",
      "properties": {
        "alertId": {
          "type": "string",
          "description": "The Opsgenie alert ID to escalate."
        },
        "escalationId": {
          "type": "string",
          "description": "The ID of the escalation policy to use."
        },
        "user": {
          "type": "string",
          "description": "Display name of the user escalating the alert."
        },
        "note": {
          "type": "string",
          "description": "Additional note to add while escalating the alert."
        }
      },
      "required": [
        "alertId",
        "escalationId"
      ]
    }
  },
  "get_opsgenie_alert": {
    "name": "get_opsgenie_alert",
    "description": "Retrieve details of an Opsgenie alert. Use this to get alert context before taking actions like acknowledging or closing. Requires a connected and enabled Opsgenie data source.",
    "parameters": {
      "type": "object",
      "properties": {
        "alertId": {
          "type": "string",
          "description": "The Opsgenie alert ID to retrieve."
        }
      },
      "required": [
        "alertId"
      ]
    }
  },
  "get_all_teams": {
    "name": "get_all_teams",
    "description": "Get all teams in the organization with their contact information and services/packages they own. Use this to understand team structure and find the right team to contact.",
    "parameters": {
      "type": "object",
      "properties": {
        "includeServices": {
          "type": "boolean",
          "description": "Include list of services owned by each team. Default: true."
        }
      },
      "required": []
    }
  },
  "get_code_reviewers": {
    "name": "get_code_reviewers",
    "description": "Get suggested code reviewers for a service or file path. Combines CODEOWNERS data with git expertise analysis to suggest the best reviewers. Returns team owners, recent contributors, and domain experts.",
    "parameters": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string",
          "description": "File path or service name to find reviewers for (e.g., \"services/payment-service/\" or \"checkout.service.ts\")."
        }
      },
      "required": [
        "path"
      ]
    }
  },
  "get_incident_contacts": {
    "name": "get_incident_contacts",
    "description": "Get escalation contacts for an incident affecting a service or feature. Returns the primary team to contact, escalation paths, related runbooks, and past incidents for context.\nPREFER: Use 'incident_response' for complete incident support - includes contacts, runbooks, past incidents, and ownership details in one call.\nUSE THIS WHEN: You need only the contact/escalation info, or when you already have runbook and incident context from other sources.",
    "parameters": {
      "type": "object",
      "properties": {
        "service": {
          "type": "string",
          "description": "Service name that is affected (e.g., \"order-service\", \"checkout\"). Can also be a feature name like \"checkout\" or \"payments\"."
        },
        "severity": {
          "type": "string",
          "description": "Incident severity: \"SEV-1\" (critical), \"SEV-2\" (high), \"SEV-3\" (medium). Affects escalation recommendations."
        }
      },
      "required": [
        "service"
      ]
    }
  },
  "get_service_ownership": {
    "name": "get_service_ownership",
    "description": "Get comprehensive ownership and contact information for a service. Returns the owning team, Slack channel, oncall contact, PagerDuty escalation, service tier, compliance requirements, and related runbooks.\nPREFER: Use 'incident_response' during incidents - it includes ownership plus escalation paths, runbooks, and similar past incidents in one call. Use 'investigate_service' for general investigation - includes ownership context.\nUSE THIS WHEN: You specifically need ownership/contact info without incident context, or when building team directory lookups.",
    "parameters": {
      "type": "object",
      "properties": {
        "serviceName": {
          "type": "string",
          "description": "Service name to look up (e.g., \"order-service\" or \"payment-service\"). Supports partial matching."
        }
      },
      "required": [
        "serviceName"
      ]
    }
  },
  "get_team_services": {
    "name": "get_team_services",
    "description": "Get all services and packages owned by a team. Returns services with their tier, packages maintained, and team contacts. Use this to understand a team's scope or find services by team.",
    "parameters": {
      "type": "object",
      "properties": {
        "teamName": {
          "type": "string",
          "description": "Team name (e.g., \"Commerce Team\", \"Platform\", \"Payments\"). Supports partial matching."
        }
      },
      "required": [
        "teamName"
      ]
    }
  },
  "acknowledge_pagerduty_incident": {
    "name": "acknowledge_pagerduty_incident",
    "description": "Acknowledge a PagerDuty incident. Requires a connected and enabled PagerDuty data source. The requesterEmail must match a valid PagerDuty user.",
    "parameters": {
      "type": "object",
      "properties": {
        "incidentId": {
          "type": "string",
          "description": "The PagerDuty incident ID to acknowledge (e.g., \"P1234567\")."
        },
        "requesterEmail": {
          "type": "string",
          "description": "Email address of the user acknowledging the incident. Must be a valid PagerDuty user email."
        }
      },
      "required": [
        "incidentId",
        "requesterEmail"
      ]
    }
  },
  "add_pagerduty_note": {
    "name": "add_pagerduty_note",
    "description": "Add a note to an existing PagerDuty incident. Requires a connected and enabled PagerDuty data source. Notes are visible on the incident timeline.",
    "parameters": {
      "type": "object",
      "properties": {
        "incidentId": {
          "type": "string",
          "description": "The PagerDuty incident ID to add a note to (e.g., \"P1234567\")."
        },
        "content": {
          "type": "string",
          "description": "The note content to add to the incident."
        },
        "requesterEmail": {
          "type": "string",
          "description": "Email address of the user adding the note. Must be a valid PagerDuty user email."
        }
      },
      "required": [
        "incidentId",
        "content",
        "requesterEmail"
      ]
    }
  },
  "resolve_pagerduty_incident": {
    "name": "resolve_pagerduty_incident",
    "description": "Resolve a PagerDuty incident. Requires a connected and enabled PagerDuty data source. The requesterEmail must match a valid PagerDuty user. Optionally include a resolution note.",
    "parameters": {
      "type": "object",
      "properties": {
        "incidentId": {
          "type": "string",
          "description": "The PagerDuty incident ID to resolve (e.g., \"P1234567\")."
        },
        "requesterEmail": {
          "type": "string",
          "description": "Email address of the user resolving the incident. Must be a valid PagerDuty user email."
        },
        "resolution": {
          "type": "string",
          "description": "Optional resolution summary describing how the incident was resolved."
        }
      },
      "required": [
        "incidentId",
        "requesterEmail"
      ]
    }
  },
  "find_equivalent_fields": {
    "name": "find_equivalent_fields",
    "description": "Find equivalent fields across repositories for a given field name. Given a field like \"windowMs\" from one repo, finds semantically equivalent fields in other repos (e.g., \"timeWindow\", \"duration\", \"Period\").\nPREFER: Use 'code_migration' for complete migration guidance including field mappings, migration examples, and ADR context in one call.\nUSE THIS WHEN: You need just the field equivalents without the full migration context, or when exploring semantic patterns across repos.",
    "parameters": {
      "type": "object",
      "properties": {
        "fieldName": {
          "type": "string",
          "description": "Field name to find equivalents for (e.g., \"windowMs\", \"maxRetries\", \"timeout\")."
        },
        "sourceRepository": {
          "type": "string",
          "description": "Repository the field comes from (helps narrow down the concept)."
        },
        "limit": {
          "type": "number",
          "description": "Maximum number of equivalent fields to return (default 15)."
        }
      },
      "required": [
        "fieldName"
      ]
    }
  },
  "get_concept_implementations": {
    "name": "get_concept_implementations",
    "description": "Get all implementations of a semantic concept across repositories. Shows how the same abstract concept is implemented with different field names, types, and interfaces in different codebases. Use this for migration guidance or to understand cross-repo field equivalents.",
    "parameters": {
      "type": "object",
      "properties": {
        "concept": {
          "type": "string",
          "description": "Concept name to get implementations for (e.g., \"time-window\", \"max-requests\"). Supports partial matching."
        },
        "repository": {
          "type": "string",
          "description": "Filter to implementations in a specific repository."
        },
        "language": {
          "type": "string",
          "description": "Filter by programming language (e.g., \"typescript\", \"go\", \"java\")."
        },
        "limit": {
          "type": "number",
          "description": "Maximum number of implementations to return (default 20)."
        }
      },
      "required": [
        "concept"
      ]
    }
  },
  "get_domain_concepts": {
    "name": "get_domain_concepts",
    "description": "Get all semantic concepts within a domain. Shows concepts with their descriptions, implementation counts, and the various names used across different repositories. Use this to understand what abstractions exist within a domain like \"rate-limiting\" or \"authentication\".",
    "parameters": {
      "type": "object",
      "properties": {
        "domain": {
          "type": "string",
          "description": "Domain name to get concepts for (e.g., \"rate-limiting\", \"authentication\"). Supports partial matching."
        },
        "limit": {
          "type": "number",
          "description": "Maximum number of concepts to return (default 30)."
        }
      },
      "required": [
        "domain"
      ]
    }
  },
  "list_semantic_domains": {
    "name": "list_semantic_domains",
    "description": "List all semantic domains discovered across repositories. Domains are high-level categories of related functionality (e.g., \"rate-limiting\", \"authentication\", \"caching\"). Use this to understand what cross-cutting concepts exist in your codebase.",
    "parameters": {
      "type": "object",
      "properties": {
        "minConceptCount": {
          "type": "number",
          "description": "Minimum number of concepts a domain must have to be included. Default is 1."
        },
        "minRepositoryCount": {
          "type": "number",
          "description": "Minimum number of repositories implementing the domain. Default is 1."
        },
        "limit": {
          "type": "number",
          "description": "Maximum number of domains to return (default 20)."
        }
      },
      "required": []
    }
  },
  "search_concepts": {
    "name": "search_concepts",
    "description": "Search for semantic concepts across all domains by keyword. Finds concepts by name, description, or alternate names used in different repos. Use this to find cross-repo equivalents for a field or configuration option.",
    "parameters": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string",
          "description": "Search query - can be a concept name (e.g., \"timeout\"), field name (e.g., \"windowMs\"), or description keyword (e.g., \"rate limit duration\")."
        },
        "limit": {
          "type": "number",
          "description": "Maximum number of concepts to return (default 15)."
        }
      },
      "required": [
        "query"
      ]
    }
  },
  "add_servicenow_work_note": {
    "name": "add_servicenow_work_note",
    "description": "Add a work note to an existing ServiceNow record (incident, change request, or problem). Requires a connected and enabled ServiceNow data source. Work notes are internal comments visible only to IT staff.",
    "parameters": {
      "type": "object",
      "properties": {
        "table": {
          "type": "string",
          "description": "The ServiceNow table name. Must be one of: \"incident\", \"change_request\", \"problem\". Other table names are not supported."
        },
        "sys_id": {
          "type": "string",
          "description": "The sys_id of the record to add the work note to."
        },
        "work_notes": {
          "type": "string",
          "description": "The work note content to add to the record."
        }
      },
      "required": [
        "table",
        "sys_id",
        "work_notes"
      ]
    }
  },
  "create_servicenow_change_request": {
    "name": "create_servicenow_change_request",
    "description": "Create a new ServiceNow change request. Requires a connected and enabled ServiceNow data source. Returns the created change request number and sys_id.",
    "parameters": {
      "type": "object",
      "properties": {
        "short_description": {
          "type": "string",
          "description": "Brief summary of the change request (one-line title)."
        },
        "description": {
          "type": "string",
          "description": "Detailed description of the change, including justification and plan."
        },
        "type": {
          "type": "string",
          "description": "Change type: \"Normal\", \"Standard\", or \"Emergency\". Defaults to \"Normal\"."
        },
        "priority": {
          "type": "string",
          "description": "Priority level: \"1\" (Critical), \"2\" (High), \"3\" (Moderate), \"4\" (Low)."
        },
        "risk": {
          "type": "string",
          "description": "Risk level: \"1\" (High), \"2\" (Medium), \"3\" (Low)."
        },
        "impact": {
          "type": "string",
          "description": "Impact level: \"1\" (High), \"2\" (Medium), \"3\" (Low)."
        },
        "category": {
          "type": "string",
          "description": "Change category (e.g., \"Software\", \"Hardware\", \"Network\")."
        },
        "assignment_group": {
          "type": "string",
          "description": "Sys ID of the assignment group for the change request."
        },
        "start_date": {
          "type": "string",
          "description": "Planned start date in ISO 8601 format (e.g., \"2025-06-15T10:00:00Z\")."
        },
        "end_date": {
          "type": "string",
          "description": "Planned end date in ISO 8601 format (e.g., \"2025-06-15T12:00:00Z\")."
        }
      },
      "required": [
        "short_description"
      ]
    }
  },
  "create_servicenow_incident": {
    "name": "create_servicenow_incident",
    "description": "Create a new ServiceNow incident. Requires a connected and enabled ServiceNow data source. Returns the created incident number and sys_id.",
    "parameters": {
      "type": "object",
      "properties": {
        "short_description": {
          "type": "string",
          "description": "Brief summary of the incident (one-line title)."
        },
        "description": {
          "type": "string",
          "description": "Detailed description of the incident. Defaults to empty if omitted."
        },
        "urgency": {
          "type": "string",
          "description": "Urgency level: \"1\" (High), \"2\" (Medium), \"3\" (Low). Defaults to \"3\"."
        },
        "impact": {
          "type": "string",
          "description": "Impact level: \"1\" (High), \"2\" (Medium), \"3\" (Low). Defaults to \"3\"."
        },
        "category": {
          "type": "string",
          "description": "Incident category (e.g., \"Software\", \"Hardware\", \"Network\")."
        },
        "assignment_group": {
          "type": "string",
          "description": "Sys ID of the assignment group to assign the incident to."
        },
        "caller_id": {
          "type": "string",
          "description": "Sys ID or user name of the caller reporting the incident."
        }
      },
      "required": [
        "short_description"
      ]
    }
  },
  "list_servicenow_incidents": {
    "name": "list_servicenow_incidents",
    "description": "List or search ServiceNow incidents. Requires a connected and enabled ServiceNow data source. Supports filtering by state, priority, assignment group, and encoded query. Returns up to the specified limit of incidents.",
    "parameters": {
      "type": "object",
      "properties": {
        "state": {
          "type": "string",
          "description": "Filter by incident state: \"1\" (New), \"2\" (In Progress), \"3\" (On Hold), \"6\" (Resolved), \"7\" (Closed). Omit to return all states."
        },
        "priority": {
          "type": "string",
          "description": "Filter by priority: \"1\" (Critical), \"2\" (High), \"3\" (Moderate), \"4\" (Low), \"5\" (Planning)."
        },
        "assignment_group": {
          "type": "string",
          "description": "Filter by assignment group sys_id."
        },
        "sysparm_query": {
          "type": "string",
          "description": "ServiceNow encoded query string for advanced filtering (e.g., \"short_descriptionLIKEnetwork^priority=1\"). Combined with individual filters (state, priority, assignment_group) when both are provided. Avoid including '&' or '=' characters that could inject additional query parameters into the URL."
        },
        "sysparm_limit": {
          "type": "string",
          "description": "Maximum number of incidents to return. Defaults to \"20\"."
        }
      },
      "required": []
    }
  },
  "update_servicenow_incident": {
    "name": "update_servicenow_incident",
    "description": "Update an existing ServiceNow incident. Requires a connected and enabled ServiceNow data source. IMPORTANT: All fields in the request body are sent to ServiceNow — omitted optional parameters are sent as empty strings, which may clear those fields. Only call this tool with ALL fields you want to preserve on the record.",
    "parameters": {
      "type": "object",
      "properties": {
        "sys_id": {
          "type": "string",
          "description": "The sys_id of the incident to update."
        },
        "state": {
          "type": "string",
          "description": "Incident state: \"1\" (New), \"2\" (In Progress), \"3\" (On Hold), \"6\" (Resolved), \"7\" (Closed)."
        },
        "short_description": {
          "type": "string",
          "description": "Updated brief summary of the incident."
        },
        "urgency": {
          "type": "string",
          "description": "Urgency level: \"1\" (High), \"2\" (Medium), \"3\" (Low)."
        },
        "impact": {
          "type": "string",
          "description": "Impact level: \"1\" (High), \"2\" (Medium), \"3\" (Low)."
        },
        "assignment_group": {
          "type": "string",
          "description": "Sys ID of the assignment group to reassign the incident to."
        },
        "assigned_to": {
          "type": "string",
          "description": "Sys ID of the user to assign the incident to."
        },
        "close_code": {
          "type": "string",
          "description": "Resolution code when resolving or closing (e.g., \"Solved (Permanently)\", \"Solved (Work Around)\", \"Not Solved\")."
        },
        "close_notes": {
          "type": "string",
          "description": "Resolution notes describing how the incident was resolved."
        }
      },
      "required": [
        "sys_id"
      ]
    }
  },
  "get_service_context": {
    "name": "get_service_context",
    "description": "Get comprehensive context for a service combining multiple data sources. Returns service info, team ownership, dependencies, dependents, code hotspots, recent incidents, relevant ADRs, and linked GitLab issues with acceptance criteria.\nPREFER: Use 'investigate_service' for the most comprehensive service analysis including all context plus documentation and flow participation in one call.\nUSE THIS WHEN: You need a quick service overview without the full documentation and flow context, or when profiling multiple services.",
    "parameters": {
      "type": "object",
      "properties": {
        "serviceName": {
          "type": "string",
          "description": "Service name to get context for (e.g., \"order-service\"). Supports partial matching."
        }
      },
      "required": [
        "serviceName"
      ]
    }
  },
  "get_service_dependencies": {
    "name": "get_service_dependencies",
    "description": "Get all dependencies of a service - what it depends on. Shows services it calls (HTTP/gRPC), packages it uses, databases it connects to, and message topics it publishes/subscribes to.\nPREFER: Use 'investigate_service' for comprehensive view including dependencies, dependents, documentation, and flows in one call.\nUSE THIS WHEN: You need only the outbound dependencies (what this service relies on), not the full service context.",
    "parameters": {
      "type": "object",
      "properties": {
        "serviceName": {
          "type": "string",
          "description": "Service name to analyze (e.g., \"order-service\"). Supports partial matching."
        },
        "relationshipType": {
          "type": "string",
          "description": "Filter to specific relationship type: \"CALLS_HTTP\", \"CALLS_GRPC\", \"USES_PACKAGE\", \"USES_DATABASE\", \"PUBLISHES_TO\", \"SUBSCRIBES_TO\". Leave empty for all dependency types."
        }
      },
      "required": [
        "serviceName"
      ]
    }
  },
  "get_service_dependents": {
    "name": "get_service_dependents",
    "description": "Get all dependents of a service - what depends on it. Shows services that call it, services that subscribe to its topics, and packages that import it.\nPREFER: Use 'blast_radius' for complete impact analysis including transitive dependents, affected flows, and teams to notify. Use 'investigate_service' if you also need the service's own dependencies.\nUSE THIS WHEN: You need only the inbound dependents (what calls this service), not the full impact analysis.",
    "parameters": {
      "type": "object",
      "properties": {
        "serviceName": {
          "type": "string",
          "description": "Service name to analyze (e.g., \"payment-service\"). Supports partial matching."
        },
        "relationshipType": {
          "type": "string",
          "description": "Filter to specific relationship type: \"CALLS_HTTP\", \"CALLS_GRPC\", \"SUBSCRIBES_TO\". Leave empty for all dependent types."
        }
      },
      "required": [
        "serviceName"
      ]
    }
  },
  "get_service": {
    "name": "get_service",
    "description": "Get detailed information about a specific service including all its relationships. Returns service metadata, owning team, dependencies (packages, databases, services it calls), and dependents (services that call it).\nPREFER: Use 'investigate_service' for comprehensive analysis that also includes documentation, business flows, and architectural context in a single call.\nUSE THIS WHEN: You need just the basic service info and relationships without the full investigation context, or when building custom queries.",
    "parameters": {
      "type": "object",
      "properties": {
        "serviceName": {
          "type": "string",
          "description": "Service name to look up (e.g., \"order-service\", \"payment-api\"). Supports partial matching."
        }
      },
      "required": [
        "serviceName"
      ]
    }
  },
  "list_services": {
    "name": "list_services",
    "description": "List all services in the knowledge graph with their metadata. Returns service name, team, tier, language, framework, and purpose. Use this to get an overview of all services or filter by team/tier/language.",
    "parameters": {
      "type": "object",
      "properties": {
        "team": {
          "type": "string",
          "description": "Filter by team name (partial match supported). Example: \"Commerce\", \"Platform\""
        },
        "tier": {
          "type": "string",
          "description": "Filter by service tier: \"critical\", \"standard\", \"internal\"."
        },
        "language": {
          "type": "string",
          "description": "Filter by programming language: \"typescript\", \"go\", \"java\", \"python\"."
        },
        "repository": {
          "type": "string",
          "description": "Filter by repository name (partial match supported)."
        },
        "limit": {
          "type": "number",
          "description": "Maximum number of services to return (default 50, max 200)."
        }
      },
      "required": []
    }
  },
  "post_slack_message": {
    "name": "post_slack_message",
    "description": "Post a message to a Slack channel. Requires a connected and enabled Slack data source. Uses the Slack chat.postMessage API. Supports threading via thread_ts parameter.",
    "parameters": {
      "type": "object",
      "properties": {
        "channel": {
          "type": "string",
          "description": "Slack channel ID (e.g., \"C1234567890\") or channel name (e.g., \"#general\")."
        },
        "text": {
          "type": "string",
          "description": "The message text to post. Supports Slack mrkdwn formatting."
        },
        "thread_ts": {
          "type": "string",
          "description": "Timestamp of the parent message to reply in a thread. Leave empty to post as a new message."
        }
      },
      "required": [
        "channel",
        "text"
      ]
    }
  },
  "update_slack_message": {
    "name": "update_slack_message",
    "description": "Update an existing Slack message. Requires a connected and enabled Slack data source. Uses the Slack chat.update API.",
    "parameters": {
      "type": "object",
      "properties": {
        "channel": {
          "type": "string",
          "description": "Slack channel ID containing the message (e.g., \"C1234567890\")."
        },
        "ts": {
          "type": "string",
          "description": "Timestamp of the message to update."
        },
        "text": {
          "type": "string",
          "description": "The new message text. Supports Slack mrkdwn formatting."
        }
      },
      "required": [
        "channel",
        "ts",
        "text"
      ]
    }
  },
  "get_major_upgrades": {
    "name": "get_major_upgrades",
    "description": "Get major version upgrades (e.g., 4.x to 5.x) that historically required code changes. These are the most impactful upgrades that often involve breaking changes and migrations. Run the upgrade-history-analyzer agent first to populate this data.\nPREFER: Use 'dependency_check' for complete package assessment including major upgrade history, security status, and migration recommendations in one call.\nUSE THIS WHEN: You need to analyze major upgrades across multiple packages, or when researching upgrade patterns organization-wide.",
    "parameters": {
      "type": "object",
      "properties": {
        "repository": {
          "type": "string",
          "description": "Filter by repository name (e.g., \"owner/repo\")"
        },
        "ecosystem": {
          "type": "string",
          "description": "Filter by ecosystem (npm, maven, pypi, go, rust)"
        },
        "limit": {
          "type": "number",
          "description": "Maximum number of upgrades to return (default 20)"
        }
      },
      "required": []
    }
  },
  "get_package_upgrade_history": {
    "name": "get_package_upgrade_history",
    "description": "Get the upgrade history for a specific package. Shows all version changes over time, whether each upgrade required code changes, and who performed them. Run the upgrade-history-analyzer agent first to populate this data.\nPREFER: Use 'dependency_check' for complete package assessment including security status, upgrade history, and migration recommendations in one call.\nUSE THIS WHEN: You need only the raw upgrade event history, or when analyzing upgrade patterns across multiple packages.",
    "parameters": {
      "type": "object",
      "properties": {
        "packageName": {
          "type": "string",
          "description": "Package name to look up (e.g., \"typescript\", \"lodash\", \"react\")"
        },
        "repository": {
          "type": "string",
          "description": "Filter by repository name (e.g., \"owner/repo\")"
        },
        "limit": {
          "type": "number",
          "description": "Maximum number of upgrades to return (default 20)"
        }
      },
      "required": [
        "packageName"
      ]
    }
  },
  "get_upgrade_summary": {
    "name": "get_upgrade_summary",
    "description": "Get a summary of upgrade analysis for a repository. Returns statistics about total upgrades, breakdown by category (with/without code changes), top upgraded packages, and upgrade sources (dependabot, renovate, manual). Run the upgrade-history-analyzer agent first to populate this data.",
    "parameters": {
      "type": "object",
      "properties": {
        "repository": {
          "type": "string",
          "description": "Repository name to get summary for (e.g., \"owner/repo\")"
        },
        "limit": {
          "type": "number",
          "description": "Maximum number of snapshots to return (default 1, latest)"
        }
      },
      "required": []
    }
  },
  "get_upgrades_with_code_changes": {
    "name": "get_upgrades_with_code_changes",
    "description": "Get upgrade events that required code changes beyond the manifest file. These are upgrades that caused friction - requiring migrations, API updates, or breaking change fixes. Useful for identifying problematic dependencies. Run the upgrade-history-analyzer agent first to populate this data.",
    "parameters": {
      "type": "object",
      "properties": {
        "repository": {
          "type": "string",
          "description": "Filter by repository name (e.g., \"owner/repo\")"
        },
        "ecosystem": {
          "type": "string",
          "description": "Filter by ecosystem (npm, maven, pypi, go, rust)"
        },
        "limit": {
          "type": "number",
          "description": "Maximum number of upgrades to return (default 30)"
        }
      },
      "required": []
    }
  },
  "get_upgrades_without_code_changes": {
    "name": "get_upgrades_without_code_changes",
    "description": "Get upgrade events that were simple version bumps with no code changes. These are clean upgrades where only manifest/lock files changed. Useful for identifying dependencies that are safe to auto-merge. Run the upgrade-history-analyzer agent first to populate this data.",
    "parameters": {
      "type": "object",
      "properties": {
        "repository": {
          "type": "string",
          "description": "Filter by repository name (e.g., \"owner/repo\")"
        },
        "ecosystem": {
          "type": "string",
          "description": "Filter by ecosystem (npm, maven, pypi, go, rust)"
        },
        "limit": {
          "type": "number",
          "description": "Maximum number of upgrades to return (default 30)"
        }
      },
      "required": []
    }
  },
  "list_upgrades": {
    "name": "list_upgrades",
    "description": "List all upgrade events discovered in a repository. Returns package name, versions, whether code changes were required, commit info, and upgrade source. Run the upgrade-history-analyzer agent first to populate this data.",
    "parameters": {
      "type": "object",
      "properties": {
        "repository": {
          "type": "string",
          "description": "Filter by repository name (e.g., \"owner/repo\")"
        },
        "ecosystem": {
          "type": "string",
          "description": "Filter by ecosystem (npm, maven, pypi, go, rust)"
        },
        "limit": {
          "type": "number",
          "description": "Maximum number of upgrades to return (default 50)"
        }
      },
      "required": []
    }
  },
  "get_workflow_guide": {
    "name": "get_workflow_guide",
    "description": "Get workflow guides for using Context Engine tools effectively. Call this at the start of a session to understand which tools to use for common workflows like investigating services, responding to incidents, assessing blast radius, managing dependencies, and exploring architecture. Returns a comprehensive guide covering all major workflows with step-by-step instructions.",
    "parameters": {
      "type": "object",
      "properties": {
        "workflow": {
          "type": "string",
          "description": "Optional filter to a specific workflow: \"investigate\", \"incident\", \"dependency\", \"architecture\", \"blast-radius\", \"search\". If omitted, returns all workflow guides."
        }
      },
      "required": []
    }
  }
}