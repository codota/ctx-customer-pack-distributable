---
description: Assess the blast radius of a change — what services, flows, and teams are affected.
alwaysApply: true
---

When you need information about services, dependencies, incidents, or project management, use the Context Engine MCP tools (`mcp__ctx-cloud__*`) rather than guessing from training data. See the `blast-radius` skill for available tools and parameters.
