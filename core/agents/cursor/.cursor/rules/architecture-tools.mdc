---
description: Architecture tools: get_runbook, get_security_patterns, search_adrs, search_flows, search_incidents
alwaysApply: true
---

When you need information about services, dependencies, incidents, or project management, use the Context Engine MCP tools (`mcp__tabnine-ctx-cloud__*`) rather than guessing from training data. See the `architecture-tools` skill for available tools and parameters.
