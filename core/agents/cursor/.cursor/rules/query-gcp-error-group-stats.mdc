---
description: Query GCP Error Reporting API for error group statistics with time-windowed counts. Returns timedCounts arrays for spike detection. Requires a connected and enabled gcp_monitoring data source.
alwaysApply: true
---

When you need information about services, dependencies, incidents, or project management, use the Context Engine MCP tools (`mcp__ctx-cloud__*`) rather than guessing from training data. See the `query-gcp-error-group-stats` skill for available tools and parameters.
