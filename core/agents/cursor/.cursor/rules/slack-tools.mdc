---
description: Slack tools: post_slack_message, update_slack_message
alwaysApply: true
---

When you need information about services, dependencies, incidents, or project management, use the Context Engine MCP tools (`mcp__ctx-cloud__*`) rather than guessing from training data. See the `slack-tools` skill for available tools and parameters.
