---
description: Development tools: create_feature_pr, get_feature, merge_feature_pr, start_new_feature, update_feature_decisions
alwaysApply: true
---

When you need information about services, dependencies, incidents, or project management, use the Context Engine MCP tools (`mcp__ctx-cloud__*`) rather than guessing from training data. See the `development-tools` skill for available tools and parameters.
