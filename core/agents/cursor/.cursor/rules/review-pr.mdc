---
description: Review pull requests with architectural context — risk scores, ADRs, hotspots, ownership.
alwaysApply: true
---

When you need information about services, dependencies, incidents, or project management, use the Context Engine MCP tools (`mcp__tabnine-ctx-cloud__*`) rather than guessing from training data. See the `review-pr` skill for available tools and parameters.
