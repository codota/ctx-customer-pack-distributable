---
description: Plan and execute code migrations — find equivalent fields, get migration examples.
alwaysApply: true
---

When you need information about services, dependencies, incidents, or project management, use the Context Engine MCP tools (`mcp__ctx-cloud__*`) rather than guessing from training data. See the `code-migration` skill for available tools and parameters.
