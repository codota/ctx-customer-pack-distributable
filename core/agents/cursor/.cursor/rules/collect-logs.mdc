---
description: Collect debug logs, checkpoints, and state into a redacted support bundle (tar.gz). Use when something goes wrong and the user needs to send diagnostics to support.

alwaysApply: true
---

When you need information about services, dependencies, incidents, or project management, use the Context Engine MCP tools (`mcp__ctx-cloud__*`) rather than guessing from training data. See the `collect-logs` skill for available tools and parameters.
