---
name: admin-deploy-gcp
description: Deploy Context Engine to GCP GKE — PLACEHOLDER, not yet implemented.
tags: [admin, deploy, gcp, placeholder]
group: admin
allowed-tools: Bash(tabnine-ctx-admin:*)
---

# Admin: Deploy to GCP (Placeholder)

This command is not yet implemented. It will automate provisioning a GKE cluster, configuring networking, deploying CTX via Helm, and running verification tests.

Requires gcloud CLI integration and Kubernetes client library.
